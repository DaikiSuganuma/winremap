Use the terminator MCP tools to test a Windows desktop app. Do exactly these steps and do not improvise extra ones.

1. Launch C:\Test\winremap.exe with the arguments: --config C:\Test\minimal.toml --lang en
2. Wait 5 seconds, then right-click the notification-area icon whose name contains "WinRemap" and click the menu item named "Show log". The icon sits directly on the taskbar — it has been promoted out of the overflow, so do NOT open the overflow flyout (that flyout does not open reliably under automation).
3. Wait 3 seconds, then read all the text in the log window and report it verbatim.

The startup lines are expected to state that WinRemap started, and that 1 keymap was loaded from C:\Test\minimal.toml.

On the final line print exactly PASS if the log window is visible and its text says one keymap was loaded from C:\Test\minimal.toml, otherwise print exactly FAIL.
