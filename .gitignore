/target/
/installer/output/
# MSIX build products. Assets/ is generated too, but committed: packaging
# should not need a Rust toolchain.
/packaging/msix/layout/
/packaging/msix/out/
# Listing images. Uploaded to Partner Center; a PNG of a window is not source,
# and the script plus the demo config beside it reproduce them. The few that
# the docs show are committed, cropped, under site/assets/screenshots/ --
# export-doc-images.ps1 makes those from these.
/packaging/msix/screenshots/
# Test VM connection details, including the guest password. Never commit.
/.secrets/
.mcp.json
*.log
*.stackdump
