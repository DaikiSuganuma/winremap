# v0.8 受け入れチェックリスト（`--debug` コンソール ＋ IME カーソル ＋ 継承）

> 元資料: [v0.8 開発計画](01_development-plan.md)、[v0.7 受け入れチェックリスト](../v0.7/03_acceptance-checklist.md)（S-1〜S-6・H-1〜H-10・P-1〜P-9 の直前の版。**MSIX 固有項目の詳しい前準備は [v0.6 §3.1](../v0.6/03_acceptance-checklist.md) が正**）、
> [ADR 0066](decisions/0066-zola-static-site.md)（ヘルプサイトの Zola 化）、[ADR 0067](decisions/0067-ime-cursor-color.md)（IME をカーソルの色で示す）、[ADR 0068](decisions/0068-debug-console.md)（`--debug` の専用コンソール）、[ADR 0069](decisions/0069-interactive-acceptance-harness.md)（この文書を読んで、この文書へ書き戻すハーネス）。
> 公式: [`SetSystemCursor`](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setsystemcursor)（セッション全体のカーソルを差し替える）／[`AllocConsole`](https://learn.microsoft.com/en-us/windows/console/allocconsole)（自前のコンソールを開く）。

- 作成日: 2026-08-02
- 作成: Claude Code（AI モデル: claude-opus-5[1m]）／**実施・記録: オーナー**（自動側の実行は Claude Code が行う）

---

## 0. この文書の目的

v0.8 は**作る側と確かめる側の作業を軽くする版**である（[開発計画 §0](01_development-plan.md)）。4 件のうち 2 件は利用者から直接は見えない。したがって確認すべきことは 3 種類ある。

1. **見える 2 件が実際に効くこと** — `--debug` のコンソール（**C-1〜C-5**）と IME のカーソル（**M-1〜M-6**）
2. **見えない 2 件が壊していないこと** — ヘルプサイトは Zola の生成物に変わった（H-9 で見る）、受け入れの手順はこのハーネスに変わった（この文書を使うこと自体が確認になる）
3. **これまでどおり動くこと** — **H-1〜H-10** と **P-1〜P-9** を継承する

### この版で特に注意すること

- **`--debug` の挙動が変わった。** ターミナルに書くのをやめ、専用のコンソールを開いて**閉じられるまで残る**（[ADR 0068](decisions/0068-debug-console.md)）。**H-10 はそのために書き換えてある** — 旧版の記述で試すと不合格に見える
- **カーソルの色はセッション全体の状態を変える。** WinRemap を殺すと**色が残る**。これは不具合ではなく設計である（[ADR 0067](decisions/0067-ime-cursor-color.md) 決定 4）。M-4 でそれ自体を確かめる
- **v0.7 から持ち越した空欄がある。** S-6（配列の切り替え）と、Store 側の P-8・P-9。[§6](#6-持ち越しと測れない項目) を先に読むこと
- **リリースビルドで通す。** `build.ps1` は既定で `-Configuration release`
- **自動テストはパッケージ版を見ていない。** 動かすのは素の `winremap.exe` である。これが P-1〜P-9 の存在理由

---

## 1. 毎リリースの手順

```powershell
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo test                              # 設定の検証・キー記法・記号キー・i18n・パス写像
.\site-src\build.ps1 -Check             # site/ が site-src/ から作った通りか（ADR 0066）
cd tests\ui
.\run-vm-ui-test.ps1                    # ゲスト VM での UI 自動テスト一式（全 10 件）
cd ..\..
cargo build --release --features test-inject
.\tests\acceptance\probe-ime-cursor.ps1 # カーソルの画素を直接読む検査（8 項目、ADR 0067）
cargo build --release                   # 受け入れは素の配布ビルドで行う
```

**自動側は Claude Code が回す。所要時間と、人の作業を邪魔するかは次のとおり**（2026-08-03 実測）。

| 手順 | 所要 | 人の作業を邪魔するか |
|---|---|---|
| `cargo fmt --check` | 1 秒 | しない |
| `cargo clippy --all-targets` | 7 秒（**初回は数分**） | しない（CPU は使う） |
| `cargo test` | 6 秒 | しない |
| `site-src\build.ps1 -Check` | 2 秒 | しない |
| `run-vm-ui-test.ps1`（全 10 件） | **26 分** | **少し邪魔する** — VMware のウィンドウが開き、ゲストの起動時に前面を取ることがある。1 件だけなら約 2 分（`-Check 05-remap-notepad`） |
| `cargo build --release [--features test-inject]` | 21 秒ずつ | しない |
| `probe-ime-cursor.ps1`（8 項目） | 21 秒 | **強く邪魔する。下記** |

**`probe-ime-cursor.ps1` の間は機械に触らないこと。** この検査は**セッション全体のマウスカーソルを差し替え**、メモ帳と `winver` を開いて**前面を何度も奪い**、IME を切り替える。しかも一方通行ではない — **人が前面を動かすと、検査のほうが誤る**。カーソルの色は前面ウィンドウの IME に従うのが仕様なので、前面が流れた先に合わせた**正しい色**を「WinRemap の不具合」と読んでしまう（2026-08-03 の受け入れで 20 回中 3 回、毎回ちがう項目が落ちた。[ADR 0067](decisions/0067-ime-cursor-color.md)「受け入れの実施で分かったこと」）。**現在は前面が保てなければ検査自身が落ちる**ようにしてあるが、その回はやり直しになる。

> したがって**自動側は席を外す時間（昼休みなど）に回すのがよい**。全部で **30 分弱**、うち 26 分が VM である。

1. 上が緑であること。**落ちたら手動へ進まない**（v0.5 以来の規律 — 自動側の判定が壊れているほうが重い）
2. 手動の項目は **Claude Code との対話で進める**（[ADR 0069](decisions/0069-interactive-acceptance-harness.md)・[ADR 0070](decisions/0070-agent-led-acceptance.md)）。Claude Code で:

```
/acceptance docs\v0.8\03_acceptance-checklist.md
```

   **司会は Claude Code が務める。** 環境の準備（設定の退避と配置、WinRemap の起動）を行い、項目を 1 件ずつ読み上げる。**人がすることは、実キーボードで打って、見えたことを自分の言葉で言うこと**だけ。4 値への分類と記録の文は Claude Code が案を出し、**確認を取ってから** [§8](#8-対話ハーネスの記録) の記録欄へ追記する。**人が写す作業は無い。**

   続きからは「続きから」（`-Resume` 相当）、一部だけなら「C-1 と M-4 だけ」と言えばよい。止めたいときはそう言えば、その場で片付け（設定を戻す）まで行う
3. **キーは実キーボードで打つこと。** ハーネスは 1 つもキーを注入しない — 注入で足りるものは `tests/ui` が既に見ている
4. §7 に、その回の全体的な判断（省略した項目とその理由など）を散文で書く
5. **Store 側（P-8・P-9）は認定通過後**に `-Resume` でもう一度回す。**順序は P-9 → P-8**（[§6.2](#62-store-側p-8p-9)。逆順にすると更新経路が測れなくなる）

---

## 2. この版で新しい項目

### 2.1 事前準備

M-* の設定は [`tests/acceptance/ime-cursor.toml`](../../tests/acceptance/ime-cursor.toml) にしてある。**退避と配置と、受け入れ用の設定での起動と、後片付けはハーネスがやる**ので、通常は何もしなくてよい（一部だけ回す場合も同じ）。C-* は設定を問わない。

手で行う場合は次のとおり。**実運用の設定は必ず退避してから**行うこと。

```powershell
Move-Item $env:APPDATA\winremap\config.toml $env:APPDATA\winremap\config.toml.bak
Copy-Item tests\acceptance\ime-cursor.toml $env:APPDATA\winremap\config.toml
# …終わったら
Move-Item -Force $env:APPDATA\winremap\config.toml.bak $env:APPDATA\winremap\config.toml
```

### 2.2 `--debug` の専用コンソール（[ADR 0068](decisions/0068-debug-console.md)）

| # | 何を見るか | 手順 | 通過条件 |
|---|---|---|---|
| **C-1** | **起動時のログが 1 行目から読める** | ターミナルで `.\winremap.exe --debug` | ①**別のコンソールウィンドウ**が開く（タイトル `WinRemap --debug`） ②`WinRemap v0.8.0 を起動しました` から**すべて**出ている ③起動元のターミナルのプロンプトと**重ならない**（v0.7 §6.3 の症状が消えている） |
| **C-2** | **終了時のログが読める** | C-1 の状態でトレイ →「終了」 | ①`[操作] 終了` とセッション終了行が出る ②**ウィンドウが消えない**（待機している） ③Enter か閉じるボタンで終わる |
| **C-3** | **窓を閉じると WinRemap も終わる** | C-1 の状態で、動作中に `--debug` の窓を閉じる | ①WinRemap が終了する（トレイから消える） ②**カーソルの色を使っていた場合、色も戻る**（[ADR 0062](../v0.6/decisions/0062-detach-console-when-resident.md) の経路） |
| **C-4** | **リダイレクトすると従来どおり** | `Start-Process .\winremap.exe -ArgumentList '--debug' -RedirectStandardOutput log.txt`（**シェルの `>` では確かめられない。下記**） | ①**窓は開かない** ②`log.txt` に同じ内容が入る ③**待たない**（プロンプトが戻る）④**常駐が続く**。この経路は UI テストが踏んでいる |
| **C-5** | **`--help` と `--version` は起動元へ** | `.\winremap.exe --help` と `--version` | 起動元のターミナルに出る（専用の窓を開かない）。[ADR 0029](../v0.2/decisions/0029-attach-console-and-tray-log-window.md) の意図を保っていること |

**C-4 でシェルの `>` が使えない理由**（2026-08-04 に判明。[ADR 0071 の訂正](decisions/0071-debug-console-must-not-kill-the-app.md#2026-08-04-の訂正決定-3-の表が誤っていた)）。WinRemap は `windows` サブシステムのバイナリで、**この 2 つは事情が違う**。

| 経路 | 標準出力が WinRemap に届くか | 結果 |
|---|---|---|
| PowerShell の `>` | 届く（パイプ） | **ファイルは空**。PowerShell は GUI サブシステムのプロセスを待たないので、常駐に入る前にパイプを閉じる |
| `cmd /c "… > log.txt"` | **届かない** | **窓が開き**、ファイルは空。cmd は `>` を自分の標準ハンドルの差し替えで実装しており `STARTF_USESTDHANDLES` を使わない。Windows は GUI サブシステムの子プロセスにその経路で標準ハンドルを渡さないので、WinRemap からは**未リダイレクトに見える** |
| `Start-Process -RedirectStandardOutput` | 届く | **通る。これで確かめること** |
| Git Bash の `>` | 届く | 通る（MSYS は `STARTF_USESTDHANDLES` で渡す） |

見分け方は `--help` で足りる — ヘルプは必ず何か出すので、**`--help` をその経路に流して 0 バイトなら、その経路では届いていない**。

### 2.3 IME をマウスカーソルの色で示す（[ADR 0067](decisions/0067-ime-cursor-color.md)）

**§2.1 の設定で行う。** `tests\acceptance\probe-ime-cursor.ps1` が画素の水準では確かめているので、ここで見るのは**人の目でしか分からないこと**である。

| # | 何を見るか | 手順 | 通過条件 |
|---|---|---|---|
| **M-1** | **IME のオン・オフで色が変わる** | メモ帳で IME をオンにし、マウスを動かす。オフに戻す | ①オンで**矢印に色が付く** ②オフで**素に戻る** ③切り替えてから色が変わるまでが**待たされる感じでない** |
| **M-2** | **入力欄の上でも変わる**（I ビーム） | 同じ状態で、テキストの上へマウスを乗せる | ①I ビームにも色が付く ②**黒い四角にならない**（形が残っている） ③**暗い背景のアプリ**（Zed など）でも**見失わない**（白い縁が効いている） |
| **M-3** | **管理者権限のウィンドウの上では変わらない**（**2026-08-03 に書き換え**） | 管理者権限のターミナルを前面にして IME をオン | **色は変わらず、パネルも出ない。** リマップが効かないのと同じ理由（UIPI）である — IME の状態は相手ウィンドウへのメッセージ送信で読むので、昇格した窓の向こうは見えない。カーソルの**描画**はセッション全体に効くが**検知**が届かない。回避には昇格が要り、不変条件 5 で禁じている（[ADR 0071](decisions/0071-debug-console-must-not-kill-the-app.md) と同じ回で判明） |
| **M-4** | **強制終了すると色が残り、次の起動で戻る** | IME をオンにして色が付いた状態で `Stop-Process -Name winremap -Force`。そのあと WinRemap を起動し直す | ①**色が残る** ②トレイに居ないことと合わせて「異常終了した」と読める ③**起動し直すと戻る**（何も操作せずに） |
| **M-5** | **独自のカーソルテーマが保たれる** | カーソルのテーマやサイズを変えている場合、色を付けてから戻す | ①色付きでも**自分のテーマの形**である ②戻したあと**元のテーマに戻る**（標準カーソルに化けない）。**テーマを変えていなければ「未実施」** |
| **M-6** | **パネルを出さずにカーソルだけ使える** | 設定を `enabled = false` ＋ `change_cursor_color = true` にして再読み込み | ①「あ」パネルは出ない ②カーソルの色は変わる（2 つが独立していること） |

---

## 3. 手動最小集合（v0.6 から継承）

**H-1〜H-9 は [v0.7 §3](../v0.7/03_acceptance-checklist.md) と同一である。H-10 だけこの版で書き換えた。** 「v0.8 での見どころ」の列が本バージョン固有で、**この版で何が変わったから何を疑うか**を書いてある。

| # | 何を見るか | 手順の要点 | v0.8 での見どころ |
|---|---|---|---|
| **H-1** | **発端の問題**が配布ビルドで直っていること | PHPStorm の内蔵ターミナル（Claude Code のプロンプト）で `C-h`。1 文字削除になる。**リリース用のバイナリ**と実キーボードで行う | 変更なし。リマップ本体にはこの版で手が入っていない — だからこそ**入っていないことの確認**である |
| **H-2** | **止まらない・遅れない** | 設定・ログウィンドウを開いたまま、普通の速さで数分間文章を打つ。取りこぼし・二重入力・遅延・スクロールの遅れが無いこと。`C-h` 長押しのリピート、Ctrl 押しっぱなしからの復帰も見る | **カーソルの色を有効にしたまま**行うこと。IME を何度も切り替えると `SetSystemCursor` がその都度走るので、**打鍵が引っかからないか**をこの版だけ意識して見る |
| **H-3** | **見た目**（1 回で全部見る） | 設定・ログウィンドウとトレイメニューを、**ライトとダークの両方**で開く。罫線・余白・アイコン・注記の印が崩れず判読でき、豆腐（□）が無いこと。日本語表示で未翻訳が無いこと | 変更なし。前回から変更が無い旨を記録に残せば省略可 |
| **H-4** | **放置時の CPU** | 両ウィンドウを閉じて数分放置し、WinRemap の CPU がほぼ 0 であること。数字で見るなら `(Get-Process winremap).TotalProcessorTime` を 5 分空けて 2 回取り、**差が数秒以内** | 変更なし。前回から変更が無い旨を記録に残せば省略可 |
| **H-5** | **IME**（1 回で全部見る） | インジケーターを有効にして、メモ帳・ブラウザ・設定アプリ（UWP）で IME をオン。表示位置・フォーカスを奪わないこと・シェル面で出ないこと。設定ウィンドウの入力欄で日本語変換ができること | **この版で最も手が入った領域。** パネルとカーソルは同じ観測から出ているので、**片方だけ正しく片方が遅れる**ことがないかを見る。タスクバーをクリックしたときに**カーソルの色が暴れない**ことも |
| **H-6** | **インストーラー経路** | インストールし、スタートメニューから起動、自動起動を有効にして再サインイン。黒い窓が一瞬も光らないこと | **`--debug` が `AllocConsole` になった版である。** 無印の起動で窓が光らないことを、この版だけ念入りに見る |
| **H-7** | **マクロ記憶**（1 回で全部見る） | 記憶 → 数文字 → 記憶終了 → 再生。再生しながら別のキーを打ってもリマップが止まらないこと。無効化・リロードで記憶が中断されること。終了後に記憶が消えていること | 変更なし |
| **H-8** | **バナー** | 記憶中のバナーが前面アプリのモニター下部中央に出て、クリックが透過し、Alt+Tab に出ないこと。**マルチモニターと管理者権限ウィンドウ**もここで見る | 変更なし |
| **H-9** | **文書** | README（en/ja）とヘルプサイトが、その版の実挙動と食い違っていないこと | **この版で大きく変わった項目。** ヘルプサイトは Zola の生成物になり、カーソルの節が増え、FAQ の `--debug` の説明が入れ替わった。[§5](#5-h-9文書で見る観点) に見る観点をまとめてある |
| **H-10** | **ターミナルから起動しても常駐が続くこと**（**この版で書き換え**） | ターミナルで `winremap.exe` を実行 → メモ帳で `C-h` が効く → **そのターミナルを閉じる** → 再び `C-h`。次に `--debug` 付きで同じことを行い、**`--debug` が開いた窓のほう**を閉じる | ①**無印は、起動元のターミナルを閉じても効き続ける**（変更なし。[ADR 0062](../v0.6/decisions/0062-detach-console-when-resident.md)） ②**`--debug` は、起動元のターミナルを閉じても効き続ける**（**v0.7 から変わった点** — 自前の窓を持ったので親から切り離されている） ③**`--debug` の窓を閉じると終了する**（C-3 と同じことを、この経路からも見る） |

> H-1・H-2・H-7 は**リマップ本体**の確認で、落ちたらリリースしない。H-3〜H-6・H-8〜H-10 は版によって影響を受けない場合があるが、**判断を毎回書き残す**こと（「前回から変更なしのため省略」も記録に残せば可 — ハーネスでは「未実施」＋理由）。

---

## 4. MSIX 固有項目（v0.6 から継承）

### 4.1 事前準備

**[v0.6 §3.1](../v0.6/03_acceptance-checklist.md) の手順がそのまま通用する。** 退避 → パッケージとデータの削除 → `build.ps1 -Register` の順で、`%APPDATA%\winremap` の有無によって通る経路が変わる（[ADR 0061](../v0.6/decisions/0061-packaged-config-path.md)）ことが理由も含めてそちらに書いてある。

> **退避したものは必ず戻す。** これはオーナーの実運用設定である。**ハーネスはこの節を自動化しない**（パッケージの登録・削除は実運用の設定を壊しうる）。手順を表示して人の確認を待つだけである。

### 4.2 項目

| # | 何を見るか | 手順の要点 | 通過条件 |
|---|---|---|---|
| **P-1** | **Store 版の初回起動** | 設定が無い状態でスタートメニューから起動。ログを開き、メモ帳で `C-h` | ①常駐する ②ログに**既定設定を作った行** ③`C-h` が 1 文字削除 |
| **P-2** | **設定パス（新規導入）** | トレイ → 設定。アドレスバーを読み、「フォルダーを開く」 | ①`…\Packages\SUGANUMADaiki.WinRemap_pktmgf1zdhxe0\LocalCache\Roaming\winremap` を指す ②Explorer が実際にそこを開く |
| **P-3** | **設定パス（インストーラー版からの移行）** | 設定を戻してから起動し直し、設定ウィンドウを開く | ①`%APPDATA%\winremap` を指す ②**自分のキーマップが出ている** ③「フォルダーを開く」でそこが開く |
| **P-4** | **スタートアップ** | 設定 → アプリ → スタートアップで WinRemap をオン → サインアウト／サインイン | ①一覧に出る ②既定は**オフ** ③オンにすると常駐し、**黒い窓が光らない** |
| **P-5** | **アンインストール** | `Get-AppxPackage -Name SUGANUMADaiki.WinRemap \| Remove-AppxPackage` | ①スタートメニューから消える ②`%LOCALAPPDATA%\Packages\…` が消える ③**`%APPDATA%\winremap` の設定は残る** |
| **P-6** | **単一インスタンス** | パッケージ版と GitHub 版を両方起動（両順序） | 2 つ目が**自力で終了**し、トレイアイコンが 1 つのまま |
| **P-7** | **管理者権限ウィンドウ** | 管理者ターミナルにフォーカスして `C-h` | リマップが**効かない**（UIPI）。GitHub 版と同じ結果。**カーソルの色は効く**（M-3） |
| **P-8** | **Store 経由インストール**（**認定通過後**） | ストアページから実際にインストールする | ①**SmartScreen の警告が出ない** ②発行元が **SUGANUMA Daiki** ③P-1 と同じ初回起動 |
| **P-9** | **更新（0.7.0 → 0.8.0）** | **Store 版**: 0.7.0 が入った状態で、認定通過後に Microsoft Store の「ライブラリ → 更新」から更新する。**GitHub 版**: 0.7.0 のインストーラーが入った状態で 0.8.0 のインストーラーを実行する | ①**設定ファイルがそのまま残る**（キーマップが消えていない） ②ログのセッション行と設定ウィンドウが **0.8.0** と出る ③**旧版が残らない**（一覧に 1 つ、常駐も 1 つ） ④**自動起動の設定が保たれる** |

> **Store 側は認定通過後にしか測れない。** ハーネスではこの 2 件を「**測れない**」＋「何が揃えば測れるか: 0.8.0 の認定通過」と記録し、通過後に `-Resume` で埋める。**リリースの完了と受け入れの完了は別**である（v0.6 §5 の教訓）。
>
> **P-9 は v0.7 で初めて実施できた項目**である。GitHub 版の上書きインストールは認定を待たずに実施できる。

---

## 5. H-9（文書）で見る観点

**「嘘になっていないか」を見る**のであって、文章の巧拙を見るのではない。

| 見るもの | 確認 |
|---|---|
| `README.md` / `README.ja.md` | IME インジケーターの節に **`change_cursor_color` / `cursor_color`** がある。**「色が残り、トレイに居ない ＝ 異常終了」**の読み方と、**何もしなくてよい**ことが書いてある。機能の一覧にカーソルの一言がある |
| ヘルプサイト `ime.html`（日英） | 同じ 3 点（設定・`enabled` と独立・異常終了の読み方）がある。**警告ブロックで扱われている** |
| ヘルプサイト `faq.html`（日英、faq3） | `--debug` の説明が**専用コンソール**のものになっている。**v0.7 で足した回避策（`Start-Process … -NoNewWindow -Wait`）が消えている**（不要になったため） |
| ヘルプサイト全体 | **`site/` を直接編集していない**こと（`site-src\build.ps1 -Check` が緑）。言語切り替えリンクが**同じページの対訳**を指す（v0.7 で見つけた FAQ の不具合の確認） |
| [02_config-spec.md](../v0.1/02_config-spec.md) §6 | `change_cursor_color`・`cursor_color` の行があり、**既定値が実装と一致**している（`#0078d4`） |
| `examples/*.toml` | `personal-ja.toml` の `change_cursor_color` が**現在の名前**である（v0.8 開発中に `cursor` から改名した） |
| リンク切れ | 下記が何も出力しないこと |

```powershell
Get-ChildItem docs -Recurse -Filter *.md | ForEach-Object {
  $dir = $_.DirectoryName
  foreach ($m in [regex]::Matches((Get-Content $_.FullName -Raw), '\]\(([^)\s]+)\)')) {
    $u = $m.Groups[1].Value
    if ($u -match '^(http|mailto:|#)') { continue }
    if (-not (Test-Path (Join-Path $dir ($u -split '#')[0]))) { "$($_.Name): $u" }
  }
}
```

---

## 6. 持ち越しと「測れない」項目

**この版で新たに決着が要る事項は無い。** 埋まっていない空欄が 2 か所ある。

### 6.1 S-6（配列の切り替え）— v0.7 から持ち越し

別配列のキーボード（JP 106 ↔ US 101）が手元に無く、v0.7 では実施できなかった。**v0.8 の作業ではない**（[開発計画 §1](01_development-plan.md)「やらないこと」）。ハーネスでは「測れない」として残す。

| # | 何を見るか | 手順 | 通過条件 |
|---|---|---|---|
| **S-6** | **配列の切り替えを再読み込みで拾う**（**キーボードがある場合のみ**） | 別配列のキーボードに差し替え、トレイ → **再読み込み**。設定ウィンドウの「使えるキー名」と `"C-;"` を見る | ①刻印の一覧が**新しいキーボードのものに変わる** ②`"C-;"` が**新しい配列の `;` のキー**に解決する ③差し替えただけで再読み込みしなければ**変わらない**（仕様どおり） |

### 6.2 Store 側（P-8・P-9）

0.8.0 の認定通過後にしか測れない。**認定を待つ間にリリースを止めない** — 通過後に `-Resume` で埋める。

**通過後は P-9 → P-8 の順で行う**（2026-08-04 に v0.7 で気づいた順序制約。[運用手順 §4.2](../03_release-operations.md)）。P-8 は**アンインストールして新規インストールする**項目なので、先に通すと **P-9 が要求する「0.7.0 が入っている状態」が消える**。Store は旧バージョンを配らないので、**0.7.0 → 0.8.0 の更新経路は二度と測れなくなる**。ハーネスがこの 2 件を出す順もこれに従うこと。

**v0.7 の Store 側 P-9 は永久に測れなくなった**（2026-08-08。[v0.7 §7](../v0.7/03_acceptance-checklist.md)）。**Store 版が 0.7.0 へ自動更新されていた**ためで、更新は起きたが誰も見ていない。**0.8.0 でも同じことが起きる** — 認定通過の連絡が来た日が、P-9 を測れる最後の日だと思って回すこと。

---

## 7. 記録欄（散文）

その回の全体的な判断を書く。**項目ごとの結果は [§8](#8-対話ハーネスの記録) にハーネスが書く**ので、ここに写さないこと。

| 日付 | 対象 | 結果 |
|---|---|---|
| 2026-08-02 | 本チェックリストの作成 | 作成。C-1〜C-5（`--debug` コンソール）と M-1〜M-6（IME カーソル）を新設し、H-10 を書き換え、H-1〜H-9 と P-1〜P-9 を継承。**[ADR 0069](decisions/0069-interactive-acceptance-harness.md) のハーネスの最初の顧客**である |
| 2026-08-03 | **C-1〜C-5 ＋ M-1〜M-6**（11 件。H-*・P-*・S-6 は未実施） | 通過 8・不合格 2（**C-4・M-3**）・未実施 1（M-5）。自動側は `cargo fmt` / `clippy` / `cargo test`（137 件）/ `site-src\build.ps1 -Check` を当日実行して緑。VM の UI テストと `probe-ime-cursor.ps1` は同日すでに回してあるというオーナー申告に拠った |
| 2026-08-04 | **C-4・M-3 の再実施 ＋ H-10・H-2**（6 件。H-1・H-3〜H-9・P-*・S-6 は未実施） | 通過 4（C-4・M-3・H-10・H-2）・不合格 2（**M-1・M-2**、いずれも 08-03 の通過を覆した）。自動側は `cargo fmt` / `clippy` / `cargo test`（137 件）/ `site-src\build.ps1 -Check` を当日実行して緑。VM の UI テストは**回していない** — 08-03 の修正は `Start-Process -RedirectStandardOutput` 経路（`stdout_is_captured` が先に効く）を踏まないため |

| 2026-08-08 | **M-1・M-2 の再実施 ＋ H-1・H-3〜H-5・H-7〜H-9 ＋ S-6・H-6 の判断**（12 件。C-*・H-2・H-10 は実施済み、P-* は未実施） | 通過 8（M-1・H-1・H-3・H-4・H-5・H-7・H-8、および一度は M-2）・**不合格 2**（**M-2**・**H-9**）・未実施 1（H-6）・測れない 1（S-6）。**M-2 は同じ日のうちに通過から不合格へ覆った**（下記）。H-9 の 2 件は当日直して `develop` へ入れた。自動側は `cargo fmt` / `clippy --all-targets` / `cargo test`（137 件）/ `site-src\build.ps1 -Check` / **`probe-ime-cursor.ps1`（8/8）** を当日実行して緑。VM の UI テストは**回していない** — H-6 と P-* をリリース工程へ回したので、次の一巡で回す |

| 2026-08-09 | **M-2 の調査と、リリースの扱いの決定**（受け入れ項目の実施は無し） | **オーナー決定: M-2 を既知の問題として文書に書いたうえで 0.8.0 を出す。** 調査で 5 つの筋を実測で否定し（下記）、原因は未特定のまま。`change_cursor_color` が**既定オフ**で、被害が「起動し直せば戻る」に限られることが判断の根拠。README（en/ja）とヘルプサイト `ime.html`（日英）に既知の問題として明記した。**次に再現したときに捕まえるための計測**（`apply()` が何をしたかを `--debug` のログに書く／検査に「画面に描かれているカーソル」の項目を足す）は実装済み。自動側は VM の UI テスト**全 10 件・121 チェック通過**、`probe-ime-cursor.ps1` **9/9**（項目が 1 つ増えた） |

### 2026-08-09 の回で分かったこと（M-2 の調査）

**5 つの筋を実測で否定した。** どれも「ありそう」に見えたが、測ると違った。

1. **他プロセスが握っているカーソルのハンドルが宙に浮く** — 別プロセスで先に `LoadCursor` した I ビームのハンドルは、`SetSystemCursor` を繰り返しても 16 秒間ずっと有効なまま。`SetSystemCursor` はハンドルを差し替えるのではなく**中身を入れ替える**
2. **`SPI_SETCURSORS` が、レジストリの `IBeam` が空だから I ビームを消す** — 打っても 26 画素のまま変わらない。**ついでに、`SPI_SETCURSORS` だけで差し替えが元に戻ることも分かった**（この機械では [`restore()`](../../src/cursor.rs) の 1 段目が無くても戻る）
3. **繰り返しの `SetSystemCursor` で中身が壊れる** — 4 回続けて差し替えても毎回 144 画素、復元も正常
4. **Zed の IME 状態が常に「オフ」と読まれている**（08-04 の記録にある観測）— **今日の実機では、Zed で IME をオンにすれば「オン」と読めている**。昨日の自動実験で「常にオフ」に見えたのは、実験が Zed 側で IME をオンにしていなかったため。**この筋は取り下げる**
5. **カーソルの種類を変えているから Zed が描けない**（素の I ビームはマスクだけ・反転で描く 32×64、着色版はカラー・アルファ付き 32×32）— **Zed が前面で IME オン、着色済みの状態でオーナーに I ビームが見えている**ことをログと目視で確認した

**自動再現は 2 通り試して、どちらも出なかった。** メモ帳 ↔ Zed を 20 往復する版と、日本語を打って変換中にしてから往復する版。**壊れた 3 回はいずれも実作業の最中**で、狙って出せたことは一度もない。

**この調査で、検査の穴が 1 つ見つかった。** `probe-ime-cursor.ps1` は**システムに登録されたカーソル**（`SetSystemCursor` が書く先）しか読んでおらず、**画面に描かれているもの**を見ていなかった。M-2 はまさにそこが割れる症状なので、8/8 通過の日に人の目には壊れて見えていたことになる。`GetCursorInfo` で実際に描かれているカーソルを測る項目を足した。

**指示の出し方でも 1 つ失敗した。** A〜D の切り分けを頼んだとき、「WinRemap は私が起動します」と書いたまま起動しておらず、**実運用の設定（カーソルの色が無効）のまま 4 つとも確かめてもらった**。青い I ビームが出るはずのない状態で「見えました」を受け取っており、設定を入れ直して測り直した。**今日の H-1 とまったく同じ形の取り違え**である — 見てもらう前に、その機能が有効な設定で動いていることを確かめること。

**H-9 の直しに 1 件足した。** ヘルプサイトの `ime.html`（日英）が「管理者権限のウィンドウの上でも色が変わります」と書いたままだった。M-3（08-03）で否定され README は直っているのに、`site-src/` への反映が漏れていた — **FAQ のリダイレクトの記述とまったく同じ落ち方**である。この版で `site-src/` が挟まったぶん、「実挙動が変わったら直す場所は README とサイトの 2 つ」が見えにくくなっている。

**同日夕、M-2 を実作業の最中に捕まえた。** 症状が出ている状態のままシステムのカーソルを測り、**入っていたのが「32×32 のカラーカーソルだが可視画素が 1 つも無い」もの**だと分かった。詳細と、否定できた 6 つの筋、確定した欠陥 2 件（**`restore()` の 1 段目がこの Windows では一度も動いていない**／**作った結果も元にする対象も検査していないため空が自己再生産される**）、v0.9 での直し方の案は [調査ノート](notes/20260809_m2-empty-ibeam.md) にまとめた。**原因の特定には至っていない。**

### 2026-08-08 の回で分かったこと

**H-* には準備が無く、実運用の設定では H-1 が測れない。** 最初の H-1 は `%APPDATA%` の設定（`notepad.exe` だけを対象にした最小設定）のまま実施したので、PHPStorm でも Zed でも WinRemap は何も変換しておらず、**素の `Ctrl+H` による単語削除＝発端の問題そのもの**が見えていた。オーナーの報告は「単語削除になりました」で、**これを WinRemap の不合格と読めば誤判定**である。実際には `phpstorm64.exe` に届く設定で起動し直せば 1 文字削除になった（[`examples/personal-ja.toml`](../../examples/personal-ja.toml) を `-c` で指定）。M-* には `-Prepare M` があるのに H-* には何も無い、というのがこの穴の正体である。**次の版でハーネスに `-Prepare H` を足すか、少なくとも H-1 の行に「どの設定で測るか」を書くこと。** 08-03 の `ime-cursor.toml` に `trigger_keys` が無かった件と同じ形の失敗である — **受け入れ用の設定がその項目に届いていないと、項目の判定を誤らせる**。

**M-2 は、直接ねらうと通り、別のことをしている最中に出た。** 項目として M-2 を見たときは 3 条件とも満たしていた（I ビームの上にカーソルを置いたまま IME をオン、Zed とメモ帳の両方）。ところが H-4 の実施中に、**メモ帳で IME をオンにしてから Zed へ移動する**という経路で再現した。同じ日のうちに通過を不合格へ覆している。新しく分かったことが 4 つある。

1. **消えるのは I ビームだけで、矢印（`OCR_NORMAL`）は青いまま正常**である
2. **「色が消える」のではなく「何も描かれない」。** 素の I ビームに戻るのとは違う
3. **一度壊れると、IME のオン・オフでも、アプリを移っても戻らない。** 起動し直すと戻る（[ADR 0067](decisions/0067-ime-cursor-color.md) 決定 5 の起動時の無条件復元）
4. **検知は正しい。** 壊れているあいだも「あ」パネルは出ており、WinRemap は「IME はオン」と読めている

同じ日の朝の `probe-ime-cursor.ps1` が 8/8 で、色付き I ビームの中身（透明 934／1024・AND マスクとアルファ一致・白い縁 64 画素）も正しい。**したがって生成側ではない。** 残るのは差し替えの経路で、**前面が変わるたびに `apply(true)` が走って同じシステムカーソルの中身を上書きし続ける**ところが疑わしい（`src/cursor.rs`）。

**M-2 は 08-03 から 3 回連続で判定が動いている**（通過 → 不合格 → 通過 → 不合格）。**画素を読む検査が 8/8 でも人の目には壊れて見える**、という状態がこの機能では起こる。次に直したときは、**再現の経路（前面をまたぐ）を `probe-ime-cursor.ps1` の項目に足してから**「直った」と言うこと。人の目に頼っているかぎり、この項目の判定は揺れ続ける。

**H-9 で、README を直した日にサイトが取り残されていたことが分かった。** FAQ（日英）は「`winremap --debug > log.txt` はファイルに書き続ける」と書いたままで、これは 08-04 に否定された記述である。同じ日に README（en/ja）は直っており、**`site-src/` への反映だけが落ちていた**。**実挙動が変わったときに直す場所は 2 つある**（README とサイト）ことが、この版で `site-src/` が挟まったぶん見えにくくなっている。リンク切れ 4 件も同時に見つかり、どちらも当日直した。

**Store 版は 0.7.0 へ自動更新されていた。** v0.7 の P-9（Store 側の 0.6.0 → 0.7.0）は**永久に測れなくなった**（[v0.7 §7](../v0.7/03_acceptance-checklist.md)）。**0.8.0 でも同じことが起きる** — 認定通過の連絡が来た日が、測れる最後の日である。

### 2026-08-03 の回で分かったこと

**リリース前に手当てが要るものが 2 つある。** どちらも `--debug` 経路で、[ADR 0068](decisions/0068-debug-console.md) がこの版で触った領域である。

1. **PowerShell の `>` でリダイレクトすると panic して常駐ごと死ぬ**（C-4、不合格）。`failed printing to stdout: パイプを閉じています (os error 232)`。PowerShell の `>` はネイティブ exe の出力をいったんパイプで受けるので、常駐に入って端末を手放した時点でそのパイプが閉じ、次の書き込みで落ちる。**`cmd /c … > file` なら通る**（常駐が続き、ログも全部入る）。stdout が消えたくらいで常駐が落ちるのは [ADR 0062](../v0.6/decisions/0062-detach-console-when-resident.md) の「端末を手放しても効き続ける」に反する
2. **`--debug` の窓を 1 回クリックするとリマップごと止まる**（項目に無い。この回の発見）。`open_debug_console()`（`src/notify.rs`）は `AllocConsole` の直後にタイトルを付けるだけで**コンソールのモードに触っていない**ため、既定で有効な **QuickEdit** の選択モードに入り、そこへの書き込みがブロックされる。書いていたスレッドが止まると低レベルフックが `LowLevelHooksTimeout`（既定 300 ms）を超え、**Windows がフックの呼び出しを捨てる** — ログが止まるだけでなくリマップが効かなくなる。**不変条件 1（フックを止めない）に直撃する。** 実際この回は、これを踏んだまま M-1 を 2 度やり直している。`SetConsoleMode` で `ENABLE_QUICK_EDIT_MODE` を落とすのが手当て

**文書側の誤りが 1 つ。** M-3 の通過条件「リマップは効かないが、カーソルは効く」は**想定が誤っている**（不合格）。カーソルの*描画*はセッション全体に効くが、IME 状態の*検知*は `SendMessageTimeoutW(WM_IME_CONTROL)` のクロスプロセス送信なので **UIPI に遮断される**。管理者権限のウィンドウが前面のあいだは色もパネルも出ない。回避には昇格が要り、それは**不変条件 5 で禁じている**。本項目と README の Limitations を書き換えること。

**受け入れ用の設定の不備を 1 つ直した。** [`ime-cursor.toml`](../../tests/acceptance/ime-cursor.toml) に `trigger_keys` が無く、**`半角/全角` キーを持たないキーボード**（`Ctrl+Space` で切り替える）では IME を切り替えても追随しなかった（[ADR 0021](../v0.1/decisions/0021-ime-indicator-trigger-keys.md) のとおりの仕様）。`trigger_keys = ["C-Space"]` と `show_app_name = true` を足して M-1 を確認し直した。**この不備は「WinRemap の不具合」に見えた** — 受け入れ用の設定は、その機械で実際に使える形になっていないと項目の判定を誤らせる。

**上の 2 件は同日中に直した**（[ADR 0071](decisions/0071-debug-console-must-not-kill-the-app.md)）。自前のコンソールで QuickEdit を切り、コンソールへの書き込み失敗で panic しないようにした。**窓の中をクリックしても止まらないことはオーナーが実際にクリックして確認済み**。PowerShell の `>` は panic しなくなったが**ファイルは空のまま**で、これは PowerShell が GUI サブシステムのプロセスを待たない性質なので直せない — C-4 の手順を `cmd /c` へ書き換えた。**§8 の C-4 は不合格のまま残してある**（書き換えた手順での再実行がまだなので、次の回に `-Resume` で戻ってくる）。M-3 の想定誤りも同日、README（en/ja）とこの表を書き換えて解消した。

**ハーネス自体について**（§0 の 2「この文書を使うこと自体が確認になる」）。[ADR 0070](decisions/0070-agent-led-acceptance.md) の対話形式で回した初回である。人が写す作業は実際に無く、11 件で記録の空欄も出なかった。**効いたのは、症状の切り分けをその場でできること**だった — C-4 は `cmd` 経由との比較を、QuickEdit はログの時刻の欠落を、M-1 は設定の読み合わせを、いずれも会話の途中で行って原因まで届いている。`Read-Host` 版なら「不合格」とだけ書いて終わっていた。

### 2026-08-04 の回で分かったこと

**前日「直した」と書いた 2 件のうち、片方は手順のほうが誤っていた。** C-4 の差し替え先に書いた `cmd /c "… > log.txt"` は通らない。cmd は `>` を自分の標準ハンドルの差し替えで実装しており `STARTF_USESTDHANDLES` を渡さないので、**Windows が GUI サブシステムの子プロセスに標準ハンドルを渡さない** — WinRemap からは未リダイレクトに見え、設計どおり窓を開き、ファイルは空のままになる。WinRemap 側に不具合は無い。正しい経路は `Start-Process -RedirectStandardOutput`（と Git Bash の `>`）で、そちらでは 4 条件とも満たした。**08-03 の調査でこの行を書いたとき、`cmd /c` を実際に通した記録が無い。確かめずに書いた 1 行が、翌日の受け入れで人の時間を使わせた**（[ADR 0071 の訂正](decisions/0071-debug-console-must-not-kill-the-app.md#2026-08-04-の訂正決定-3-の表が誤っていた)）。判定は `--help` を流すだけで足りる — **0 バイトなら届いていない**。

**QuickEdit と panic の手当ては効いていた。** 誤った `cmd /c` 経路を踏んだこと自体が、期せずしてその確認になっている — 前日はここで `os error 232` の panic とともにトレイから消えていたのに、この回は常駐が続いた。

**H-2 の実施中に、カーソルの色そのものの不具合が 2 件出た。** どちらも「打鍵が引っかからないか」を見ている最中に**目で見つけた**もので、項目としては H-2 は通過している。

1. **IME がオンのままカーソルの色だけ消える**（M-1、不合格）。変換途中でマウスカーソルを窓の外へ出し、確定してから戻すと、IME はオンなのに色が付いていない。IME を打ち直すと戻る。**筋の通らない箇所は見つかった** — `detect::query_foreground()` の `open` は 3 値で `None` は「オフ」ではなく「分からなかった」と `detect.rs` に明記してあるのに、呼び出し側が `== Some(true)` でその区別を捨て、不明のときに色を外していた（[ADR 0072](decisions/0072-cursor-keeps-tint-when-state-unknown.md) で修正）。**ただしそれが原因だと確かめたわけではない。** 修正後の再現では、4 分間のログに「不明」が 1 行も出なかった。**M-1 は次の回で再確認するまで不合格のままにしてある**
2. **IME をオンにすると I ビームが消える**（M-2、不合格）。Zed でもメモ帳でも、I ビームの状態から IME をオンにすると I ビームが見えなくなり、オフにすると見える。矢印のほうは色が付く。**08-03 に M-2 を通過としたのは誤判断だった** — そのとき同じ症状を「入力すると I ビームが消える」と受け取り、Windows が打鍵中にポインターを隠す仕様だと解釈した。今回**打鍵しなくても起きる**ことが分かって覆った。原因は未特定。画面に触らない読み取り検査で `restore()` が入れ直す stock の I ビームは系のものと同一（`mask=32x64/1bpp`・color 無し）と確かめ、**「復元が I ビームを壊す」説は否定した**。残る筋は `SPI_SETCURSORS` と色付きカーソルの作り置きの競合だが、裏は取れていない

**「再現しない」を結論にしないこと。** この回はどちらも再現しなかったが、**オーナーが 2 度見ている**ものを再現待ちで消さず、不合格のまま残した。カーソルの色は**間欠的にしか壊れない**種類の機能である（前面・IME・他プロセスの応答性に依存する）ので、記録のほうを証拠として扱う。

**分かった副産物。** ログを見ると **Zed は IME の状態が常に「オフ」と読まれている**（09:13:58・09:15:13）。M-2 の原因かどうかは別として、独自に入力を処理するアプリでは検知が届かないことがある、という材料である。次の版で見ること。

---

## 8. 対話ハーネスの記録

[`tests/acceptance/run-acceptance.ps1`](../../tests/acceptance/run-acceptance.ps1) が下のマーカーの間に追記する（[ADR 0069](decisions/0069-interactive-acceptance-harness.md)・[ADR 0070](decisions/0070-agent-led-acceptance.md)）。見出しには**その回の環境**（キーボード配列・版・OS）と、**誰が答えたか**が刻まれる。**手で書き換えないこと** — 追記のみで、同じ項目を後日やり直せば新しい行が増える。「測れない → 認定通過後に通過」という経緯が読めることが、この欄の値打ちである。

<!-- harness:begin -->
#### 2026-08-03 — キーボード: Japanese（00000411） / WinRemap 0.8.0 / Microsoft Windows 11 Pro（26200）（対話: 人が実キーボードで確認し、AI エージェントが記録）

| 項目 | 結果 | 記録 |
|---|---|---|
| C-1 | 通過 | Windows Terminal の PowerShell から `--debug` 起動。タイトル `WinRemap --debug` の別窓が開き、1 行目の「起動しました」から出ていて、起動元のプロンプトも崩れなかった |
| C-2 | 通過 | トレイ →「終了」で `[操作] 終了` とセッション終了行が出て、待機の案内を出したまま窓が残った。Enter で閉じた |
| C-4 | 不合格 | PowerShell の `>` でリダイレクトすると、窓は開かずプロンプトも返るが直後に `failed printing to stdout: パイプを閉じています (os error 232)` で panic。`log.txt` は空、トレイからも消える。`cmd /c … > file` なら通る（常駐が続きログ 5 行が起動行から入る）ので、引き金は PowerShell の `>` がパイプを閉じること。stdout の書き込み失敗で常駐ごと落ちるのは ADR 0062 に反する |
| C-5 | 通過 | `--help` と `--version` はどちらも起動元の Windows Terminal に出た（専用の窓を開かない）。ただし `AttachConsole` の帰結でプロンプトと文字が重なって読みにくい（ADR 0029 の既知の帰結。`--debug` 側は v0.8 で解消済み） |

#### 2026-08-03 — キーボード: Japanese（00000411） / WinRemap 0.8.0 / Microsoft Windows 11 Pro（26200）（対話: 人が実キーボードで確認し、AI エージェントが記録）

| 項目 | 結果 | 記録 |
|---|---|---|
| C-3 | 通過 | `--debug` で起動し、メモ帳で IME をオンにしてカーソルが青い状態で窓を「×」で閉じた。WinRemap が終了し、カーソルの色も戻った |
| M-1 | 通過 | `Ctrl+Space` で切り替え。オンで矢印が青くなり、オフで素に戻り、切り替えた瞬間に変わる（前面を行き来する必要なし）。ただし受け入れ用の設定に `trigger_keys` が無いと追随せず、`半角/全角` キーの無いキーボードでは `trigger_keys = ["C-Space"]` が要る（ADR 0021）。設定に足してから確認した |
| M-2 | 通過 | メモ帳と暗い背景のアプリで確認。I ビームにも青が付き、I の形が残り、暗い背景でも見失わない。打鍵中に I ビームが消えるのは Windows の「入力中にポインターを非表示にする」仕様 |
| M-3 | 不合格 | 管理者権限の Windows Terminal とメモ帳で `Ctrl+Space`。カーソルの色は変わらず、パネルも出ない（他のアプリから戻っても同じ）。リマップも効かない。状態検知が `SendMessageTimeoutW(WM_IME_CONTROL)` のクロスプロセス送信で UIPI に遮断されるため。通過条件「カーソルは効く」の想定が誤り — 描画はセッション全体だが検知は届かない。README の Limitations と本項目の書き換えが要る（昇格は不変条件 5 で禁止） |
| M-4 | 通過 | メモ帳で IME をオンにして色が付いた状態で `Stop-Process -Force`。色が残ることを確認し、起動し直したら何も操作せずに色が戻った |
| M-5 | 未実施 | カーソルのテーマ・サイズを変えていない機械（スキーム `Windows Aero`、サイズ 32 の既定のまま）。チェックリストの指示どおり未実施 |
| M-6 | 通過 | `enabled = false` ＋ `change_cursor_color = true` で起動して確認。パネルは出ず、カーソルの色だけが `Ctrl+Space` に追随した（2 つが独立している） |

#### 2026-08-04 — キーボード: Japanese（00000411） / WinRemap 0.8.0 / Microsoft Windows 11 Pro（26200）（対話: 人が実キーボードで確認し、AI エージェントが記録）

| 項目 | 結果 | 記録 |
|---|---|---|
| C-4 | 通過 | `Start-Process -RedirectStandardOutput` 経路で再実施し 4 条件とも満たした。①窓は開かない（MainWindowTitle は空、子の conhost も 0 個） ②log に起動行から 5 行・442 バイト ③プロンプトはすぐ戻る ④常駐が続き、オーナーが「タスクトレイにアイコンが出ていて」「コントロール H で 1 文字削除されることを確認しました」。**前回この項目に書いた `cmd /c "… > file"` は誤った手順だった** — cmd の `>` は STARTF_USESTDHANDLES を使わず、Windows は GUI サブシステムの子に標準ハンドルを渡さないので、WinRemap から見ると未リダイレクトになり窓が開きファイルは空になる（`--help` でも 0 バイト）。PowerShell の `>` はファイルが空になる点は変わらないが、**今日の修正で panic せず常駐が続くことを確認**（cmd /c 経路でも PID が生き続けた）。届く経路は `Start-Process -RedirectStandardOutput` と Git Bash の `>` の 2 つ |

#### 2026-08-04 — キーボード: Japanese（00000411） / WinRemap 0.8.0 / Microsoft Windows 11 Pro（26200）（対話: 人が実キーボードで確認し、AI エージェントが記録）

| 項目 | 結果 | 記録 |
|---|---|---|
| M-3 | 通過 | 管理者権限の Windows Terminal とメモ帳で `Ctrl+Space`。カーソルの色は変わらず、パネルも出ない（他のアプリから戻っても同じ）。リマップも効かない。書き換え後の通過条件どおりの挙動である。原因は `SendMessageTimeoutW(WM_IME_CONTROL)` のクロスプロセス送信が UIPI に遮られること。2026-08-03 の観測を、通過条件の書き換えに合わせて分類し直したもの（挙動は同じで、今回の修正は UIPI に無関係） |

#### 2026-08-04 — キーボード: Japanese（00000411） / WinRemap 0.8.0 / Microsoft Windows 11 Pro（26200）（対話: 人が実キーボードで確認し、AI エージェントが記録）

| 項目 | 結果 | 記録 |
|---|---|---|
| H-10 | 通過 | ①無印を Windows Terminal の PowerShell から起動。メモ帳で `C-h` が効き、起動元のターミナルを閉じたあとも効き続けた ②`--debug` 付きで同じことを行い、`WinRemap --debug` の窓ではなく**起動元のターミナル**を閉じても効き続けた（v0.7 から変わった点 — 自前の窓を持ったので親から切り離されている） ③`--debug` の窓を閉じると WinRemap が終了した。実運用の設定（メモ帳の `C-h` だけ）のまま実施 |

#### 2026-08-04 — キーボード: Japanese（00000411） / WinRemap 0.8.0 / Microsoft Windows 11 Pro（26200）（対話: 人が実キーボードで確認し、AI エージェントが記録）

| 項目 | 結果 | 記録 |
|---|---|---|
| H-2 | 通過 | 受け入れ用の設定（カーソルの色を有効）のまま、設定・ログウィンドウを開いて数分間入力。取りこぼし・二重入力・遅延・ログのスクロール遅れは無し。`C-h` 長押しのリピート、Ctrl 押しっぱなしからの復帰も正常。`Ctrl+Space` で IME を何度も切り替えながら打っても打鍵は引っかからない — `SetSystemCursor` が毎回走ることによる打鍵への影響は見られなかった。ただしこの回の途中で、カーソルの色そのものに 2 件の不具合を見つけた（M-1・M-2） |
| M-1 | 不合格 | **IME オンのまま色が消える経路がある。** メモ帳で IME オン → 日本語を入力 → 変換途中でマウスカーソルをウィンドウの外へ出す → 変換を確定 → カーソルをメモ帳の上へ戻す。このとき IME はオンなのにカーソルに色が付いていない。IME をオフ・オンし直すと再び色が付く。色を外す経路は `apply(false)` → `restore()` だけなので、この間に「IME はオフ」と読んでいる。変換中の候補ウィンドウが前面を取り、そちらの IME 状態を読んでいる疑い（ADR 0021 の再取得契機は前面変化と `trigger_keys` のみ）。2026-08-03 の通過を、この観測で覆したもの |
| M-2 | 不合格 | **IME をオンにすると I ビームが消える。** Zed で I ビームの状態から IME をオンにすると I ビームが見えなくなる。IME をオフにすると I ビームは見える。メモ帳でも同じ（I ビームの状態から IME をオンにすると消える）。矢印のほうは色が付く。2026-08-03 の記録にある「入力すると I ビーム自体が消える」を Windows の仕様（打鍵中はポインターを隠す）と解釈したのは誤りで、**打鍵しなくても IME をオンにしただけで消える**。色付き I ビームの生成（`from_mask_only` → `outlined` → `and_mask`）がこの機械で中身の無いカーソルを作っている疑い。2026-08-03 の通過を、この観測で覆したもの |

#### 2026-08-08 — キーボード: Japanese（00000411） / WinRemap 0.8.0 / Microsoft Windows 11 Pro（26200）（対話: 人が実キーボードで確認し、AI エージェントが記録）

| 項目 | 結果 | 記録 |
|---|---|---|
| M-1 | 通過 | メモ帳で `Ctrl+Space` により切り替え。①オンで矢印に色が付く ②オフで素に戻る ③待たされる感じは無い、の 3 条件とも満たした。08-04 に不合格の原因となった経路（IME オン → 日本語を入力 → 変換途中でマウスカーソルをメモ帳の外へ出す → 確定 → メモ帳の上へ戻す）も試し、色は消えなかった。ADR 0072（状態が不明のときは色を動かさない）を入れた後の再実施で、08-04 の不合格を覆したもの |
| M-2 | 通過 | Zed（暗い背景）とメモ帳の両方で確認。I ビームの上にカーソルを置いたまま IME をオンにするという、08-04 に落ちたのと同じ引き金で試し、①I ビームに色が付く ②形が残る（黒い四角にならない）③Zed の暗い背景でも見失わない、の 3 条件とも満たした。同日 probe-ime-cursor.ps1 が 8/8 通過し、色付き I ビームの中身（透明 934／1024・AND マスクとアルファが一致・白い縁 64 画素）も正しい。08-04 の不合格は再現しなかったが、原因は特定されていない |

#### 2026-08-08 — キーボード: Japanese（00000411） / WinRemap 0.8.0 / Microsoft Windows 11 Pro（26200）（対話: 人が実キーボードで確認し、AI エージェントが記録）

| 項目 | 結果 | 記録 |
|---|---|---|
| H-1 | 通過 | examples\personal-ja.toml（オーナー自身の設定）で 0.8.0 を -c 指定で起動し、PHPStorm の内蔵ターミナルの Claude Code のプロンプトで Ctrl+H → 1 文字削除。Zed と Windows Terminal でも 1 文字削除になった。最初の実施は実運用の設定（%APPDATA% の config.toml。notepad.exe だけを対象にした最小設定）で行ったため、PHPStorm・Zed では WinRemap が何も変換しておらず、素の Ctrl+H による単語削除＝発端の問題そのものが見えていた（Windows Terminal だけは同アプリ側の sendInput 設定で 1 文字削除）。設定を phpstorm64.exe に届く形にしたうえでの再実施で通過。H-* には -Prepare が無く、実運用の設定では H-1 が測れないことがここで分かった |
| H-4 | 通過 | 設定・ログウィンドウを閉じた状態で、タスクマネージャーで WinRemap の CPU がほぼゼロであることをオーナーが確認した。TotalProcessorTime の 5 分間差分は取っていない |
| M-2 | 不合格 | H-4 の実施中に再現した。メモ帳で IME をオンにして青い I ビームを確認したあと Zed へ移動すると、I ビームがまったく見えなくなる（色が消えるのではなく何も描かれない）。このとき Zed の上に「あ」パネルは出ており、WinRemap は IME をオンと読めている。メモ帳へ戻しても消えたままで、IME のオン・オフをやり直しても戻らない。矢印（OCR_NORMAL）は青いまま正常に見えるので、壊れるのは I ビーム（OCR_IBEAM）だけ。IME オフのときの素の I ビームは見える。同日の受け入れ開始時に probe-ime-cursor.ps1 が 8/8 通過しており、色付き I ビームの中身（透明 934／1024・AND マスクとアルファ一致・白い縁 64 画素）は正しい。したがって生成側ではなく、前面が変わるたびに apply(true) が走ってシステムカーソルの中身を上書きし続ける経路が疑わしい。同日先に記録した「通過」を、この観測で覆したもの |

#### 2026-08-08 — キーボード: Japanese（00000411） / WinRemap 0.8.0 / Microsoft Windows 11 Pro（26200）（対話: 人が実キーボードで確認し、AI エージェントが記録）

| 項目 | 結果 | 記録 |
|---|---|---|
| H-3 | 通過 | ライトテーマとダークテーマの両方で確認し、罫線・余白・アイコン・注記の印の崩れ、豆腐（□）、未翻訳のいずれも無し。examples\personal-ja.toml（Emacs 風の全アプリキーマップ・記号キー・IME の節を含む設定）で開いた。設定ウィンドウ・ログウィンドウ・トレイメニューのどれを開いたかは、確認が流れたため記録できていない |
| H-5 | 通過 | メモ帳・ブラウザ・設定アプリ（UWP）で IME をオンにし、①パネルがアクティブウィンドウの中央に出る ②フォーカスを奪わない ③シェル面では出ない ④設定ウィンドウの入力欄で日本語変換ができる、の 4 点とも正常。この回は青い I ビームも消えず正常に表示され、パネルとカーソルで片方だけ遅れる様子も無かった（M-2 の症状は出なかった。同じ日に出る回と出ない回がある） |
| H-7 | 通過 | 記憶の開始・終了 S-F10、再生 F10、delay_ms = 8 の設定で実施。①記憶 → 数文字 → 記憶終了 → F10 で再生 ②再生しながら別のキーを打ってもリマップが止まらない ③無効化・リロードで記憶が中断される ④終了後に記憶が消えている、の 4 点とも正常 |
| H-8 | 通過 | 記憶中のバナーが①前面アプリのモニターの下部中央に出る ②クリックが透過する ③Alt+Tab に出ない ④マルチモニターと管理者権限のウィンドウでも同じ、の 4 点とも正常 |

#### 2026-08-08 — キーボード: Japanese（00000411） / WinRemap 0.8.0 / Microsoft Windows 11 Pro（26200）（対話: 人が実キーボードで確認し、AI エージェントが記録）

| 項目 | 結果 | 記録 |
|---|---|---|
| H-9 | 不合格 | 2 件見つかった。①ヘルプサイトの FAQ（site/faq.html・site/ja/faq.html の faq3）が「出力をリダイレクトした場合（winremap --debug > log.txt）は、ウィンドウを開かずそのファイルに書き続けます」と書いており、2026-08-04 に否定された記述のまま残っていた。PowerShell の > はファイルが空になり、cmd /c の > は窓が開いてやはり空になる。届くのは Start-Process -RedirectStandardOutput と Git Bash の > だけ。README（en/ja）は 08-04 に直っていて、site-src/ への反映だけが漏れていた ②docs/ にリンク切れが 4 件（v0.8/03_acceptance-checklist.md の 0021 が ../v0.2/ を指す、0071 が存在しない 0016-debug-log-post-thread-message.md を指す、0072 が存在しない 0023-ime-indicator-uwp-child-window.md と ../../v0.3/ の 0033 を指す）。通ったのは、README（en/ja）のカーソルの節と異常終了の読み方、ime.html 日英の change_cursor_color・cursor_color と警告ブロック、FAQ から v0.7 の回避策（-NoNewWindow -Wait）が消えていること、site-src\build.ps1 -Check が緑、言語切り替えリンクが同じページの対訳を指すこと、02_config-spec.md §6 の既定値 #0078d4 が実装と一致すること、personal-ja.toml の設定名が現在のものであること |

#### 2026-08-08 — キーボード: Japanese（00000411） / WinRemap 0.8.0 / Microsoft Windows 11 Pro（26200）（対話: 人が実キーボードで確認し、AI エージェントが記録）

| 項目 | 結果 | 記録 |
|---|---|---|
| S-6 | 測れない | 別配列のキーボード（JP 106 ↔ US 101）が手元に無い。v0.7 から続けての持ち越しで、v0.8 の作業でもない（開発計画 §1「やらないこと」）。別配列のキーボードが 1 台あれば測れる |
| H-6 | 未実施 | リリース工程で行うとオーナーが判断した。インストーラーはリリース時にどのみち作るので、そのときに P-1〜P-9 とまとめて回す。今日は M-2 が不合格で残っており、リリースまでにもう一巡することが確定しているため、ここでインストールと再サインインを挟む値打ちが無いという判断 |

#### 2026-08-09 — キーボード: Japanese（00000411） / WinRemap 0.8.0 / Microsoft Windows 11 Pro（26200）（対話: 人が実キーボードで確認し、AI エージェントが記録）

| 項目 | 結果 | 記録 |
|---|---|---|
| H-9 | 通過 | 3 件直したうえでの再実施。①FAQ（日英）のリダイレクトの記述を実挙動に合わせた ②ime.html（日英）の「管理者権限のウィンドウの上でも色が変わります」を、UIPI で検知が届かないという実挙動に直した ③設定リファレンス config.html（日英。README が「Every option」として指すページ）に change_cursor_color / cursor_color が載っていなかったので足した。②③はどちらも、README を直した日に site-src/ への反映が漏れていたもので、①と同じ落ち方である。通ったのは、README（en/ja）のカーソルの節・異常終了の読み方・既知の問題、ime.html 日英の設定と独立性と警告ブロックと既知の問題、v0.7 の回避策（-NoNewWindow -Wait）が消えていること、site-src\build.ps1 -Check が緑、言語切り替えリンクが同じページの対訳を指すこと、02_config-spec.md §6 の既定値が実装と一致すること、docs/ のリンク切れが 0 件であること |

<!-- harness:end -->

---

## 9. 記録の書き方

v0.5 の記録欄（[§6](../v0.5/03_acceptance-checklist.md)）が見本である。**「OK」だけを書かない。** 何をどう確かめたか、省略したなら省略した判断を残す。落ちた項目は、落ちた事実と原因の切り分け結果まで書く。**ハーネスは 4 種類すべてで記録を必須にしている**ので、空欄では先へ進めない。

この版で特に残す価値があるのは次の 3 つである。

- **M-2 を、実際にどのアプリの、どんな背景で見たか。** 「見えた」ではなく「Zed のダークテーマの本文の上で、白い縁が見えた」と書く。この項目は**背景の明るさによって答えが変わる**ので、明るいアプリだけで見た記録は次の版で使えない
- **C-1〜C-3 を、どのターミナルで行ったか。** `--debug` の重なりは PSReadLine の再描画が原因だった（[v0.7 §6.3](../v0.7/03_acceptance-checklist.md)）ので、**Windows Terminal / PowerShell / cmd のどれで見たか**に意味がある
- **省略した項目と、その判断。** 「前回から変更なし」も判断である。埋まっていない空欄がどこにあるかが分かるほうが、埋まったふりより価値がある
