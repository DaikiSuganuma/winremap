# WinRemap 開発計画（v0.8）

> 元資料: オーナー宣言（2026-08-02、v0.7.0 のリリース直後）「v0.8 では次のことを行う」の 4 件。決定済みの 2 件は [ADR 0066](decisions/0066-zola-static-site.md)（Zola・生成物をコミット）と [ADR 0067](decisions/0067-ime-cursor-color.md)（`SetSystemCursor`）。
> 関連: [ADR 0029](../v0.2/decisions/0029-attach-console-and-tray-log-window.md)（`--debug` の出力先。**Phase C はこれを覆す**）、[v0.7 受け入れチェックリスト §6.3](../v0.7/03_acceptance-checklist.md)（重なる件の経緯）、[05_ui-test-automation.md](../05_ui-test-automation.md)（自動テストの現状）、[04_git-branching.md](../04_git-branching.md)（ブランチとバージョン番号）。
> Rust 実装の作法は [02_rust-guidelines.md](../02_rust-guidelines.md)。

- 作成日: 2026-08-02
- 作成: Claude Code（AI モデル: claude-opus-5[1m]）／**スコープ承認: オーナー（2026-08-02）**
- ADR の連番は v0.7 の 0065 に続き **0066 から**（0066・0067 は作成済み、0068・0069 は Phase C・D の着手時）

---

## 0. このバージョンの性格

v0.7 が**利用者から見た欠落**（記号キー）を埋めた版だったのに対し、v0.8 は**作る側と確かめる側の作業を軽くする版**である。4 件のうち 2 件（Zola 化・受け入れの対話化）は利用者から直接は見えない。

見える 2 件（IME のカーソル・`--debug` のコンソール）にも共通点がある。**どちらも「状態が見えない」ことへの手当て**である — IME がオンかどうか、起動時と終了時に何が起きたか。

---

## 1. スコープ（**オーナー宣言 2026-08-02**）

| # | 項目 | 状態 |
|---|---|---|
| A | **ヘルプサイトの静的サイトジェネレーター化** | 方式決定済み（[ADR 0066](decisions/0066-zola-static-site.md)）— Zola、生成物をコミット |
| B | **IME 切り替え時にマウスカーソルの色を変える** | 方式決定済み（[ADR 0067](decisions/0067-ime-cursor-color.md)）— `SetSystemCursor`。**異常終了時の扱いは承認待ち** |
| C | **`--debug` を `AllocConsole` にする** | **ADR 0068 が要る**（ADR 0029 を覆す） |
| D | **受け入れテストの対話化** | **ADR 0069 が要る**（形と記録先の決定） |

### やらないこと

- **v0.7 で未実施のまま残した S-6**（配列の切り替え）は、**別配列のキーボードが手に入ったとき**に行う。v0.8 の作業ではない
- **`site/` の文言そのものの改訂**は Phase A に混ぜない。移行と改訂を混ぜると差分が読めなくなる

### 最初にやること

`Cargo.toml` の `version` を **`0.8.0`** に上げる（[04_git-branching.md §2.6](../04_git-branching.md) — 番号はリリース時ではなく開発開始時に上げる）。

---

## 2. Phase A — ヘルプサイトの Zola 化

**決定は [ADR 0066](decisions/0066-zola-static-site.md) にある。** 原稿は `site-src/`、出力は現在の `site/`、`pages.yml` は無変更。

### タスク

1. `site-src/` に Zola プロジェクトを起こす（`config.toml`・`templates/`・`static/`）。**現行の Bootstrap の体裁をテンプレートへ移す**
2. 既存 7 ページ ×2 言語を `content/` へ移植する。日本語が原稿、英語が対訳
3. `zola build` の出力先を `site/` にし、生成物をコミットする
4. `ci.yml` に「`zola build` して `git diff --exit-code -- site` が通ること」のジョブを足す（**生成物のコミット忘れを赤にする**）
5. `.gitattributes` に `site/** linguist-generated=true`

### 完了条件

- 生成された `site/` が**移行前と表示上同じ**であること（日英・ライト/ダーク）
- Pages が従来どおり発行され、`privacy.html` が 200 を返す（Store 提出の順序制約に効く）
- **1 か所の修正が 1 か所の編集で済む**ようになっていること

---

## 3. Phase B — IME の状態をカーソルの色で示す

**決定は [ADR 0067](decisions/0067-ime-cursor-color.md) にある。** `SetSystemCursor` で `OCR_NORMAL` と `OCR_IBEAM` を差し替え、復元は `SPI_SETCURSORS`。

### この Phase の重さは「色を変えること」ではない

**セッション全体の状態を触るので、WinRemap が死んだあとにも残る。** ADR 0067 の決定 3〜5（panic / 未処理例外 / `WM_ENDSESSION` での復元、痕跡ファイルによる次回起動時の検出と通知、状態の組み合わせの文書化）が実装量の大半である。

### タスク

1. `src/cursor.rs` を新設し、`unsafe` をそこに閉じ込める。**AGENTS.md 不変条件 3 の許可リストを改訂**する（前例: [ADR 0031](../v0.2/decisions/0031-notify-module-unsafe-allowlist.md)）
2. 差し替えと復元。**`CopyIcon` した複製を渡す**（`SetSystemCursor` はハンドルを破棄する）
3. 既存の IME 通知経路（[ADR 0033](../v0.2/decisions/0033-ime-status-across-input-threads.md)）から呼ぶ。**フックには手を入れない**
4. 終了経路 5 種の復元（ADR 0067 決定 3 の表）
5. 痕跡ファイルと、次回起動時の復元 ＋ 異常終了の通知
6. 設定項目（**既定はオフ**）と、設定ウィンドウでの色の選択
7. 文書化 — 「色が変わったままでトレイに居なければ異常終了」（README・ヘルプサイト）

### 完了条件

- IME のオン・オフでカーソルの色が変わる（**入力欄の上でも**変わること — I ビームを差し替えている理由）
- `Stop-Process -Force` で殺すと色が残り、**次に起動すると戻り、異常終了したことが知らされる**
- 独自のカーソルテーマを使っている環境で、復元後に**そのテーマに戻る**

---

## 4. Phase C — `--debug` を `AllocConsole` にする（**ADR 0068 が要る**）

### なぜやるか

オーナーの目的は「**起動時の挙動や終了時の挙動をログ表示で確認する**」ことである。現状はこれができない。

- 起動元のターミナルに書いているので、**シェルのプロンプトと出力が重なる**（[v0.7 §6.3](../v0.7/03_acceptance-checklist.md)。ADR 0029 の既知の帰結で、WinRemap 側だけでは直せない）
- ログウィンドウは**起動後にしか開けない**ので、起動時のログは追えない

### ADR 0068 で決めること

[ADR 0029](../v0.2/decisions/0029-attach-console-and-tray-log-window.md) は `AllocConsole` を「ログウィンドウがあるので不要」として却下している。**覆すには、当時と何が変わったかを書く必要がある** — 変わったのは「起動時と終了時が見たい」という用途が出てきたことである。

決めるべき点:

1. **終了時のログをどう残すか。** `AllocConsole` の窓は**最後のプロセスが離れた瞬間に消える**。終了処理のログを出しても読めない。「閉じるまで残す」「キー入力を待つ」など、**明示的な設計が要る**（ここを決めないと、この Phase は目的を果たさない）
2. **窓を閉じられたときの扱い。** `CTRL_CLOSE_EVENT` が来て WinRemap も落ちる。デバッグ用途なら許容だが、決めて書くこと
3. **リダイレクトされているときは従来どおりにする。** UI テストは `--debug` の出力をファイルへ流して読んでいる。**ここを壊すと自動テストが全滅する**
4. **[H-10](../v0.7/03_acceptance-checklist.md) の意味が変わる** — 「`--debug` は端末を閉じると終了する」が成立しなくなる。受け入れ項目を書き換える

---

## 5. Phase D — 受け入れテストの対話化（**ADR 0069 が要る**）

### 何を解くのか

v0.7 の受け入れで実際に起きたことが、そのまま要件である。

- **配列の記録が空のまま残った。** S-1〜S-5 は通ったが、「どの機械のどのキーで確かめたか」が書かれていない。記号キーは**機械によって答えが変わる機能**なので、この記録が無いと次の版で使えない
- **測れない項目が混ざる。** P-8・P-9 の Store 経路は認定通過後にしか測れず、「通過」と書くと嘘になる
- **手順とチェックリストと記録欄が別々の場所にある。** 人が写す作業が発生し、写し漏れる

### 形（案）

`tests/ui` の資産を流用し、**1 項目ずつ進む対話ハーネス**にする。

1. **環境を準備する**（設定の退避と配置、対象アプリの起動、ログウィンドウを開く）— ここは自動
2. **何を打つかを提示する**（「メモ帳で `abc` と打ち、`Ctrl` を押しながら `;` のキー」）
3. **人が実キーボードで打つ** — **ここは絶対に注入しない。** 実キーボードで確かめること自体が目的である
4. **結果を選ぶ**（通過 / 不合格 / 未実施）＋ **自由記述**（「US 101 の `;`（`VK_OEM_1`）」のような、記録に残す価値のある一文）
5. **チェックリストの §7 に追記する** — 人が写さない

### ADR 0069 で決めること

- **記録先**（チェックリストの Markdown を直接書き換えるか、中間ファイルを経由するか）
- **「未実施」と「測れない」を区別できる形**（後者は認定待ちのように**後で埋まる**）
- ハーネス自体をどこに置くか（`tests/acceptance/` に [`symbol-keys.toml`](../../tests/acceptance/symbol-keys.toml) がある）

---

## 6. Phase E — 文書と受け入れ・リリース

- README（en/ja）とヘルプサイトに **B の設定項目と「異常終了の見分け方」**を書く。**Phase A の後**に書く（Zola へ移ったあとの原稿に書かないと二度手間になる）
- 受け入れチェックリスト（`docs/v0.8/03_acceptance-checklist.md`）は **Phase D のハーネスの最初の顧客**にする
- リリースは [03_release-operations.md](../03_release-operations.md) のとおり。チャネルは **GitHub Releases・Microsoft Store・winget の 3 つ**

---

## 7. フェーズ順序と依存関係

```
Phase A（Zola 化）──────────────┐
                                ├──▶ Phase E（文書・受け入れ・リリース）
Phase C（AllocConsole）──┐      │
                         ├──▶ Phase B（IME カーソル）──┘
Phase D（対話ハーネス）──┘
```

- **C を先に**やる。小さく閉じており、**以降の開発とデバッグが楽になる**（起動時と終了時が見えるようになる）
- **A は Rust に触らない**ので、C・B と並行できる
- **B は C の後**が望ましい。異常終了まわりの確認は、起動時・終了時のログが見えるほうが速い
- **D は B より前**に形だけ用意できると、B の受け入れ（プロセスを殺してカーソルが残ることの確認）をそのハーネスで行える

---

## 8. オーナー判断（**要・承認**）

| # | 事項 | 状態 |
|---|---|---|
| 1 | 静的サイトジェネレーターは **Zola**、**生成物をコミットする** | **決定済み（2026-08-02）** → [ADR 0066](decisions/0066-zola-static-site.md) |
| 2 | カーソルの色は **`SetSystemCursor`** | **決定済み（2026-08-02）** → [ADR 0067](decisions/0067-ime-cursor-color.md) |
| 3 | **異常終了の検出方法** — 痕跡ファイル ＋ 次回起動時の復元と通知、「色が残り、トレイに居ない」を異常終了の意味として文書化 | **承認待ち**（ADR 0067 決定 3〜5） |
| 4 | **`--debug` の終了時ログをどう残すか** — 窓はプロセス終了と同時に消える | **未決**（ADR 0068） |
| 5 | 対話ハーネスの**記録先とハーネスの置き場所** | **未決**（ADR 0069） |
| 6 | 4 件を**この順序**（C → A・B → D）で進めてよいか | **承認待ち**（§7） |
