# WinRemap 開発計画（v0.6）

> 元資料: [v0.5 開発計画](../v0.5/01_development-plan.md) §1 見送り表（記号キー対応を v0.6 の目玉として扱う提案）、[ADR 0060](decisions/0060-msix-package.md)（MSIX 構成と Phase 0 実測）、Microsoft の [SmartScreen reputation for Windows app developers](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation)（配信経路の判断根拠）。
> Rust 実装の作法は [02_rust-guidelines.md](../02_rust-guidelines.md)、ブランチ運用は [04_git-branching.md](../04_git-branching.md) を参照。

- 作成日: 2026-07-29
- 作成: Claude Code（AI モデル: claude-opus-5[1m]）／レビュー・**承認: オーナー（2026-07-29）**
- ADR の連番は v0.5 の 0058 に続き **0059 から**（0059〜0061 は着手済み）

---

## 0. このバージョンの性格

v0.6.0 は**機能ではなく配布経路のバージョン**である。動機は 1 つしかない。

> **「Windows によって PC が保護されました」**

利用者がダウンロードした `winremap.exe` / `winremap-setup.exe` に SmartScreen の警告が出る。これを消したい、というオーナーの要望（2026-07-29）が起点である。

調査の結果、**この警告を確実に消せる経路は Microsoft Store だけ**だと分かった。Microsoft の公式ドキュメントは、非 Store 配信では署名しても警告は消えない（評判はダウンロード数で蓄積する）と明示し、かつて即座に信頼を得られた EV 証明書についても *"EV certificates no longer bypass SmartScreen"* と否定している。Store 配信では Microsoft がパッケージを再署名するため、**証明書は不要かつ購入不可**である。

したがって本バージョンのスコープは **MSIX パッケージ化と Microsoft Store への提出**となる（オーナー決定 2026-07-29）。

### v0.5 の提案からの変更

[v0.5 開発計画 §1](../v0.5/01_development-plan.md) の見送り表は、**記号キー（OEM キー）対応を「v0.6 以降で単独のバージョンの目玉として扱う」**ことを提案していた。本バージョンではこれを行わない（オーナー決定 2026-07-29）。

理由は、SmartScreen 警告が**今この瞬間も全ての新規利用者に出続けている**のに対し、記号キー対応は既存利用者の機能拡張だからである。前者は導入の入口そのものを塞いでおり、優先度が上である。記号キー対応は **v0.7 の目玉**として送る。**3 回目の先送りは認めない**（v0.5 が回帰確認で経験した構造）。

---

## 1. スコープ（**承認済み 2026-07-29**）

### 採用

| # | 項目 | 位置づけ |
|---|---|---|
| A | **MSIX パッケージング基盤** — マニフェスト・アセット・ビルドスクリプト・パッケージ実行時の設定パス | 土台。**着手済み・完了** |
| B | **掲載情報とスクリーンショット** — Partner Center に投入する文面と画像 | 提出物。**完了** |
| C | **既存記述の Store 対応** — SECURITY.md・README（en/ja）・ヘルプサイト | ドキュメント。配布経路が増えることの反映。**完了** |
| D | **リリース運用手順への MSIX 追加** — [03_release-operations.md](../03_release-operations.md) | 運用。次バージョン以降の再現性 |
| E | **受け入れテスト** — MSIX 固有項目の新設と、v0.5 の最小集合の継承 | 品質 |
| F | **リリースと Store 提出** | 完了 |

### 見送り（理由を記録）

| 候補 | 見送り理由 |
|---|---|
| **記号キー（OEM キー）対応** | §0 のとおり。**v0.7 の目玉として単独で扱う。次は送らない** |
| **多言語ファイル化**（Project Fluent / ICU4X） | v0.3〜v0.5 と同じ判断を継続。本バージョンは Rust コードの変更が最小（`src/package.rs` 1 モジュールのみ）で、UI 文字列もほとんど増えないため移行の好機ではあるが、利用者に見える変化がゼロの作業をリリース経路の変更と同時にやると、問題が起きたときの切り分けが難しくなる。**v0.7 で記号キー対応と同時に判断する** |
| **Azure Artifact Signing による GitHub 版の署名** | Store 版と違い、署名しても SmartScreen 警告は消えない（評判の蓄積が要る）。しかも個人開発者は米国・カナダ在住者に限られ、日本の個人は対象外である。GitHub 直配布版の警告を消したい場合は [Certum のオープンソース証明書](https://support.certum.eu/en/code-signing-required-documents/)（年 €29）が候補になるが、**Store 版が出た後に効果を見てから判断する** |
| **`RuntimeBehavior="win32App"` への変更** | 仮想化を避けられるが Store 提出での受理が未確認（[ADR 0061](decisions/0061-packaged-config-path.md) 却下案）。アプリ側で辻褄を合わせる方式が既に動いているため、変える理由がない |
| **UI テストの CI 組み込み** | v0.4・v0.5 と同じ（VMware とゲスト認証情報がホスト固有） |
| **winapp CLI への移行**（[検証計画](../v0.5/notes/20260727_winapp-cli-migration.md) Phase 1 ゲート） | v0.5 から持ち越しの**オーナー承認待ち**。本バージョンの主題と独立しており、判断を待つ間も A〜F は進む |
| **scoop 再申請** | 知名度基準未達（[ADR 0048](../v0.3/decisions/0048-scoop-defer-extras.md)）。状況が変わっていない。**ただし Store 掲載で知名度が動く可能性があるため、v0.7 で再評価する** |
| **winget 更新自動化** | v0.4・v0.5 と同じ。手作業の更新を続ける |
| tap/hold・マークモード・ウィンドウタイトルによる切り替え・expand-region | v0.2〜v0.5 と同じ理由で見送る |

### v0.5 からの持ち越し（未解決）

| 事項 | 状態 |
|---|---|
| **ログウィンドウを開いている間、切り替えた先のアプリが前面アプリ行に出ない** | [ADR 0058](../v0.5/decisions/0058-log-readability.md) 末尾・[05_ui-test-automation.md](../05_ui-test-automation.md) の落とし穴節に測定ごと記録済み。**開発機では再現しなかった**（下記）。**v0.7 でログ周辺に触れるときに扱う** |

#### 再現確認（2026-07-29、開発機・リリースビルド）

Phase B の撮影で、ログウィンドウを開いた状態の打鍵がすべて「素通し」になり、この持ち越しが**表示だけでなく変換結果を狂わせる不具合**なのではないかと疑った。ログウィンドウのテキストを UI Automation で読む形で測ったところ、**そうではなかった**。

```
13:33:27.861  launching notepad
13:33:28.039  application = "notepad.exe" — matching keymaps: notepad, everywhere
```

ログウィンドウを開いたまま Notepad を起動しても、前面アプリの変化は **0.2 秒で報告**され、適用キーマップも正しく解決されている。

素通しの原因は撮影スクリプト側だった。Notepad を起動しただけでフォーカスが移ると仮定していたが、**フォアグラウンド権を持たないプロセスから起動したウィンドウを Windows は前面にしない**。打鍵は前面のままだったウィンドウ（`winremap.exe`）に入り、そこに適用される `everywhere` キーマップに `C-h` が無いため、**正しく素通しされていた**。

v0.5 の観測は VM の UI テスト環境で得られたものであり、本測定は開発機である。環境が違うため**観測を取り消しはしない**が、少なくとも開発機のリリースビルドでは再現しないことを記録する。

方針はブリーフどおり **1 機能 1 ADR**、各フェーズ着手前にオーナー承認を得る。

---

## 2. Phase A — MSIX パッケージング基盤 — **完了（2026-07-29）**

### 2.1 やったこと

| # | 内容 | ADR |
|---|---|---|
| 1 | **初回起動時に既定の設定ファイルを作る** | [0059](decisions/0059-first-run-creates-the-default-config.md) |
| 2 | **MSIX パッケージの構成**（マニフェスト・アセット生成・ビルドスクリプト） | [0060](decisions/0060-msix-package.md) |
| 3 | **パッケージ実行時の設定パス解決** | [0061](decisions/0061-packaged-config-path.md) |

1 は**着手前には見えていなかった前提の欠落**だった。WinRemap は設定ファイルが無いと起動を拒否しており、それが問題にならなかったのは Inno インストーラーがファイルを置いていたからである。MSIX にはインストール時スクリプトが無いため、**このままでは Store 版の初回起動が必ず失敗していた**。

### 2.2 Phase 0 実測（実機）

| 検証項目 | 結果 |
|---|---|
| パッケージ登録 | ✅ PFN が Partner Center 発行の `SUGANUMADaiki.WinRemap_pktmgf1zdhxe0` と一致 |
| 起動とキーボードフックの設置 | ✅ 常駐。ADR 0027・0045 が却下理由にしていた AppContainer の懸念は成立しない |
| **キー変換の実動作** | ✅ オーナー確認済み（2026-07-29） |
| 初回起動時の設定生成 | ✅ 動作 |
| 単一インスタンス防止 | ✅ 2 つ目は自力で終了 |
| 設定パスの解決（移行の分岐） | ✅ 既存設定を使い、Explorer で開ける |
| 設定パスの解決（リダイレクト分岐） | ✅ リダイレクト先を報告することを実測 |

### 2.3 この Phase で分かった、以後に効くこと

- **リダイレクトは読み書きに関して完全に透過**である。ファイル配置を外から観察しても修正の前後を区別できない。区別できるのは「アプリが何と表示するか」だけで、**エラーメッセージがそれを外から読める唯一のテキスト出力**である（ADR 0061 §確認方法）
- ADR 0027・0045 が MSIX を却下した根拠（証明書必須・AppContainer の相性）は**両方とも成立しない**

---

## 3. Phase B — 掲載情報とスクリーンショット — **完了（2026-07-29）**

### 3.1 状態

文面は[起草済み](notes/20260729_store-listing.md)（日英・`runFullTrust` の理由説明・ストアポリシー 10.2 への備えを含む）。**残りはスクリーンショットの撮影**である。

### 3.2 タスク

1. 撮影方法を決める（§3.3）
2. 6 枚を撮る（構成案は草案 §4）
3. Partner Center の各欄の**実際の文字数上限**を確認し、超過があれば詰める

### 3.3 撮影方針（**オーナー判断**）

掲載画像は公開物なので、**デスクトップ全体のキャプチャは行わない**。開いているウィンドウやファイル名が写り込む。2 案ある。

| 案 | 方法 | 長所 | 短所 |
|---|---|---|---|
| **1（推奨）** | 開発機で `PrintWindow` によりウィンドウ単体を取得し、無地のキャンバスに合成 | 速い。画面上の他の情報が一切入らない。解像度を自由に決められる | 合成スクリプトを書く必要がある |
| 2 | UI テスト VM（`tests/ui/run-vm-ui-test.ps1` の `winremap-test`）の素のデスクトップから撮る | 配慮が不要。デスクトップごと撮れる | VM の起動と受け渡しの手間 |

設定ウィンドウは推奨解像度（1366×768）より小さいため、**どちらの案でも合成は必要**になる。

### 3.4 完了条件

- Partner Center の要件（1366×768 以上）を満たしている
- **個人情報・第三者の情報が 1 ピクセルも写っていない**
- 各欄の文面が上限内に収まっている

### 3.5 実施記録（2026-07-29） — **完了**

日英各 4 枚、計 8 枚を 1920×1080 で取得した（`packaging/msix/capture-screenshots.ps1`）。

| # | 画像 | 内容 |
|---|---|---|
| 01 | 設定ウィンドウ | キーマップ 3 件のツリー、ルール表、コメント列、キー記法パネル |
| 02 | 設定ウィンドウ（編集モード） | 編集中バナー、行の追加・削除、入力欄ごとの読み仮名 |
| 03 | ログウィンドウ | 置換・マクロ（3 ストローク）・2 ストロークの 3 種類が 1 枚に |
| 04 | トレイメニュー | 2 倍拡大 |

撮影用に専用の設定（`packaging/msix/screenshot-demo.{en,ja}.toml`）を用意した。`examples/` の設定は手本であって画像の仕事とは別であり、オーナー個人の設定は公開画像に名前が載る。

#### この Phase で分かったこと（以後に効く）

**1 枚の画像を撮るのに 5 回かかった。** 原因はすべて撮影スクリプト側で、アプリは一貫して正しく動いていた。記録に値するのは、いずれも「自動化が黙って間違った結果を出す」形だった点である。

| 失敗 | 原因 |
|---|---|
| ログが空 | メニュー位置を上から探る実装が、途中で **Enabled を選んでリマップを OFF** にしていた |
| 全キーが「素通し」（2 回） | `SetForegroundWindow` は**フォアグラウンド権が無いと何もせず成功を返す**。Notepad は裏のままで、打鍵は前面の別ウィンドウに入り、そこのキーマップで正しく素通しされていた |
| 前面待ちが機能せず | プロセスが見つからない場合に確認ごとスキップし、見つかっても 30 秒で諦めて続行していた |
| 他ウィンドウの写り込み | 画面から矩形を切り出すため、手前に何かあれば入る。**Z オーダー固定**（フォアグラウンド権不要）で解決 |
| ログにノイズ | 実行中にスタートメニューから Notepad を開くと `searchhost.exe` 等の行が入る。**起動前に開いておく**方式に変更 |

**合成入力はリマップされない**（不変条件 1）ため、ログの内容は人が打つしかない。この 1 枚だけは自動化できない。

**アプリ側の疑いは晴れた。** 一時は「ログウィンドウを開いていると変換が効かない」不具合を疑ったが、実測で否定した（§1 の再現確認）。

---

## 4. Phase C — 既存記述の Store 対応 — **完了（2026-07-29）**

配布経路が 1 つ増えることで、**現在の記述が嘘になる箇所**がある。v0.4 で「前バージョンの記述が 6 箇所残ったままリリース準備に入った」教訓（v0.5 §5）を踏まえ、棚卸しとして扱う。

### 4.1 タスク

1. **SECURITY.md** — 「公式バイナリは GitHub Releases のみ」という記述に Microsoft Store を加える。**これは重要な変更である**。この一文は「他サイトのものは非公式」という判断基準を利用者に与えており、Store 版を出した瞬間に不正確になる
2. **README（en/ja）** — インストール導線に Store を追加。英語が正、`README.ja.md` を追随（AGENTS.md ワークフロー 7）
3. **`site/install.html`（en/ja）** — Store からの導入手順。あわせて SmartScreen の断り書きを「GitHub から直接落とした場合」に限定する
4. **全ページのフッターにプライバシーポリシーへのリンク**を追加（現在は `privacy.html` 自身にしかない）
5. **`site/index.html` の FAQ**（en/ja）に「Store 版と GitHub 版の違い」を追加。設定ファイルの場所が違う点を含める

### 4.2 完了条件

- Store が配布経路として全ての利用者向け文書に載っている
- **前バージョンの記述で嘘になっているものが無い**
- 設定ファイルの場所が版によって違うことが利用者向けに説明されている

### 4.3 実施記録（2026-07-29） — **完了**

§4.1 の 5 項目をすべて実施した。加えて棚卸しの過程で 2 件見つかった。

| # | 対象 | 内容 |
|---|---|---|
| 1 | `SECURITY.md` | 「公式は GitHub Releases のみ」を**表**に置き換えた。経路ごとに「誰が署名するか」「どう検証するか」を並べ、Store 版は署名者が Microsoft、検証手段はストアページの発行元表示（SUGANUMA Daiki）と製品 ID であることを書いた。**同じソース・同じタグから作られており、選択は信頼の問題ではない**ことを明記した |
| 2 | `README.md` / `README.ja.md` | クイックスタート 1 を「経路を選ぶ」構成に変更。セキュリティ節も 2 経路に。**Store 版の設定ファイルの場所**を引用ブロックで補足した |
| 3 | `site/install.html`（日英） | 冒頭に**比較表**（警告・更新・設定ファイル・ポータブル可否）を置き、Store／GitHub の節に分けた。SmartScreen の警告文を「GitHub からダウンロードしたファイルの話」に限定し、Store なら出ないことを添えた。末尾に「設定ファイルの置き場所」節を新設 |
| 4 | サイト全 12 ページ | フッターにプライバシーポリシーへのリンクを追加（従来は `privacy.html` 自身にのみ存在） |
| 5 | `site/index.html`（日英） | FAQ に「Microsoft Store 版と GitHub 版は何が違う?」を追加（faq10）。既存の faq8（保存場所）・faq9（検証）も Store 版に対応させた。ヒーローに Store ボタンを追加 |
| **＋1** | `site/config.html`（日英） | 「設定ファイルの形」の冒頭が `%APPDATA%` を断定していた。Store 版の但し書きを 1 段落追加 |
| **＋2** | `site/install.html`（日英） | **`examples/suganuma.toml` へのリンクが 404 だった。** v0.5 で `personal-ja.toml` に改名した際の取りこぼしで、日英どちらのページにも残っていた |

＋2 は本フェーズの主題とは無関係だが、**まさにこのフェーズが探しに行った種類の不具合**である（「前バージョンの記述が残ったままリリースに入る」— v0.4 の教訓）。棚卸しとして扱った判断が当たった形なので記録する。

#### Store リンクの扱い

`https://apps.microsoft.com/detail/9N6TQDXRX5WV` は**認定通過まで解決しない**。しかし §7.1 の順序制約により、文書のほうが先に公開される。

リンクを伏せる案も採ったが、そうすると「経路は 2 つ」と言いながら片方に行けない文書になる。**リンクは書き、その場に「v0.6.0 の認定通過後に公開される」と添える**方式にした。winget を「マニフェストの受理後」と書いている既存の書き方と同じ扱いである。

Phase F の完了時に、この但し書きを消す作業が **6 か所**ある。

| ファイル | 箇所 |
|---|---|
| `SECURITY.md` | 引用ブロック 1 件 |
| `README.md` / `README.ja.md` | 引用行の括弧書き 各 1 件 |
| `site/install.html` / `site/ja/install.html` | Store ボタン直下の `small` 1 件 |
| `site/index.html` / `site/ja/index.html` | ヒーローの `small` 1 件 |

#### スクリーンショットの二次利用（オーナー要望 2026-07-29）

Phase B で撮った画像を **GitHub の README にも載せる**ことにした。Store 用は 1920×1080 の無地キャンバスに合成してあり（Partner Center の最小解像度要件）、README では余白の分だけウィンドウが小さく写る。そこで `packaging/msix/export-doc-images.ps1` を追加した。**同じ撮影結果**から余白を切り詰めた版を作り、`site/assets/screenshots/` へ書き出す。

- 撮り直しではなく**同じ画像を加工する**ので、Store 掲載画像と README の画像が食い違うことがない
- 置き場所を `site/assets/` にしたのは、GitHub Pages が `site/` を公開し、README の相対パスはリポジトリ上で解決されるため。**1 つの実体で両方をまかなえる**
- 採用は設定ウィンドウとログウィンドウの 2 枚（日英計 4 枚）。編集モードとトレイメニューは掲載情報側の素材であり、README では説明の重複になる

---

## 5. Phase D — リリース運用手順への MSIX 追加

[03_release-operations.md](../03_release-operations.md) に、次バージョン以降も再現できる手順として書く。

1. `packaging\msix\build.ps1 -Pack` で `.msix` を作る（**署名しない**）
2. Partner Center へアップロードし、掲載情報とパッケージのバージョンが一致していることを確認する
3. `AppxManifest.xml` の `Version` は `build.ps1` が `Cargo.toml` から埋めるため**手で触らない**。第 4 フィールドは Store が使うので常に 0
4. 認定に落ちた場合の再提出の流れ

あわせて、§4 の winget 更新手順の隣に置き、**リリース 1 回で更新すべきチャネルが 2 つになった**ことを明示する。

---

## 6. Phase E — 受け入れテスト

`docs/v0.6/03_acceptance-checklist.md` を新設する。構成は v0.5 を踏襲し、**MSIX 固有の項目を足す**。

### 6.1 継承するもの

[v0.5 受け入れチェックリスト §4](../v0.5/03_acceptance-checklist.md) の**手動最小集合 H-1〜H-9**（所要 30〜40 分）と、自動側（`cargo test` ＋ `run-vm-ui-test.ps1` 全 9 件）をそのまま引き継ぐ。

### 6.2 追加する MSIX 固有項目（案）

| # | 項目 | 確認内容 |
|---|---|---|
| P-1 | Store 版の初回起動 | 設定が無い状態から既定設定が作られ、リマップが効く |
| P-2 | 設定パス（新規） | 設定ウィンドウのパスが実体を指し、「フォルダーを開く」で Explorer が開く |
| P-3 | 設定パス（移行） | インストーラー版の設定がある状態で Store 版を入れると、その設定が使われる |
| P-4 | スタートアップ | Windows の「設定 → アプリ → スタートアップ」に出る。ON にするとサインイン時に起動する |
| P-5 | アンインストール | パッケージ専用フォルダーが消え、`%APPDATA%` の設定は残る |
| P-6 | 単一インスタンス | Store 版と GitHub 版を同時に起動しても 1 つしか常駐しない |
| P-7 | 管理者権限ウィンドウ | UIPI の制限がパッケージ版でも同じであること（既知の制限として掲載文に書いた内容の裏取り） |

P-1〜P-3・P-6 は Phase A で一度確認済みだが、**リリースビルドで通し直す**（Phase A の実測は debug ビルドである）。

### 6.3 完了条件

- P-1〜P-7 と H-1〜H-9 が**リリースビルドで**通っている
- 自動側が全件緑

---

## 7. Phase F — リリースと Store 提出

### 7.1 順序制約（重要）

**プライバシーポリシー URL は `main` にリリースするまで 404 である。** GitHub Pages は `main` からのみ発行される（`.github/workflows/pages.yml`）。Partner Center は有効な URL を要求するため、**Store 提出は v0.6.0 のリリース後**になる。

`main` に入れてよいのは `release/*` と `hotfix/*` だけ（[04_git-branching.md §2.5](../04_git-branching.md)）なので、抜け道は作らない。

### 7.2 手順

1. `release/0.6.0` を切る。`Cargo.toml` が `0.6.0` であることを**確認するだけ**（開発開始時に上げ済み）
2. Phase E の受け入れを通し、記録する
3. `main` へマージし、`v0.6.0` タグ。GitHub Releases のドラフトに本文を入れてオーナーへ引き渡す
4. **Pages の発行を確認する** — `https://daikisuganuma.github.io/winremap/privacy.html` が 200 を返すこと
5. `build.ps1 -Pack` で `.msix` を作り、Partner Center へ提出する
6. winget マニフェストを 0.6.0 に更新して提出する（**チャネルが 2 つになった**）
7. 認定通過後、**「認定通過後に公開されます」の但し書きを 6 か所から消す**（§4.3 に一覧。URL 自体は既に実 URL である）

### 7.3 完了条件

- v0.6.0 が GitHub Releases に出ている
- Microsoft Store の認定を通過し、`https://apps.microsoft.com/detail/9N6TQDXRX5WV` から導入できる
- **Store からインストールした場合に SmartScreen の警告が出ないことを実機で確認した**（本バージョンの目的そのもの）

---

## 8. フェーズ順序と依存関係

```
Phase A（MSIX 基盤）【完了】
   │
   ├──▶ Phase B（掲載情報・スクリーンショット） ──┐
   ├──▶ Phase C（既存記述の Store 対応） ─────────┤
   └──▶ Phase D（リリース運用手順） ──────────────┤
                                                  │
                                Phase E（受け入れ）◀┘
                                        │
                                        ▼
                     Phase F（リリース → Pages 発行 → Store 提出）
```

- **B・C・D は互いに独立**。並行して進められる
- **Phase C は Phase F より前に完了していること**。リリース後に文書を直すと、公開された版の記述が嘘のまま残る（v0.4 の教訓）
- **Phase F の 4→5 の順序は動かせない**（§7.1）

---

## 9. オーナー判断 — **全件承認済み（2026-07-29）**

| # | 事項 | 決定 |
|---|---|---|
| 1 | 上記スコープ（§1）の採否 | **承認**。配布経路に振る |
| 2 | 記号キー対応を v0.7 の目玉とすること | **承認**。**v0.8 へは送らない** |
| 3 | スクリーンショットの撮影方法（§3.3） | **承認: 案 1**（開発機＋ウィンドウ単体合成） |
| 4 | 掲載言語を日英 2 言語とするか | **承認: 2 言語** |
| 5 | GitHub 直配布版に Certum の証明書を入れるか | **承認: 本バージョンでは行わない**。Store 版の効果を見てから v0.7 で判断する |
| 6 | winapp CLI 移行の Phase 1 ゲート（v0.5 持ち越し） | 本バージョンとは独立。いつでも判断可能 |
