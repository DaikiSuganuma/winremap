# v0.9 受け入れチェックリスト（後始末の版 — 直したものと、動かした土台）

> 元資料: [v0.9 開発計画](01_development-plan.md)、[v0.8 受け入れチェックリスト](../v0.8/03_acceptance-checklist.md)（C-1〜C-5・M-1〜M-6・H-1〜H-10・P-1〜P-9 の直前の版。**MSIX 固有項目の詳しい前準備は [v0.6 §3.1](../v0.6/03_acceptance-checklist.md) が正**）、
> [ADR 0073](decisions/0073-restore-from-a-snapshot-taken-at-startup.md)（カーソルの復元を起動時の複製から行う）、[ADR 0074](decisions/0074-egui-0-36-and-the-upstream-pr.md)（egui 0.36.1 と upstream への PR）、[ADR 0069](../v0.8/decisions/0069-interactive-acceptance-harness.md)・[ADR 0070](../v0.8/decisions/0070-agent-led-acceptance.md)（この文書を読んで、この文書へ追記するハーネス）。
> 公式: [`CopyIcon`](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-copyicon)（起動時に採る複製）／[`SetSystemCursor`](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setsystemcursor)（セッション全体のカーソルを差し替える）／[Generating MSIX package components](https://learn.microsoft.com/en-us/windows/msix/desktop/desktop-to-uwp-manual-conversion)（`makepri` と unplated アセット）。

- 作成日: 2026-08-11
- 作成: Claude Code（AI モデル: claude-opus-5[1m]）／**実施・記録: オーナー**（自動側の実行は Claude Code が行う）

---

## 0. この文書の目的

**v0.9 は利用者から見える新機能を入れない版である**（[開発計画 §1](01_development-plan.md)、オーナー決定 2026-08-09）。したがってこの受け入れは、ほぼ全部が「**変わっていないこと**」の確認になる。ただし**変わっていないことを確かめるのが難しい版**でもある。

1. **直したものが直っていること** — M-2（カーソルが空になる）と、MSIX のアイコン（**P-10**）
2. **土台を動かしたのに何も壊れていないこと** — egui が **0.35.0 → 0.36.1** に上がった。GUI 全体がその上に乗っている
3. **これまでどおり動くこと** — C-1〜C-5・M-1〜M-6・H-1〜H-10・P-1〜P-9 を継承する

### この版で特に注意すること

- **M-2 は「直したはず」の項目である。** v0.8 で 4 回落ち、**原因は依然として未特定**。入れたのは「空が利用者に届かないようにする」手当てと「安全網を実際に効かせる」直しであって、壊れる契機を突き止めたわけではない（[ADR 0073](decisions/0073-restore-from-a-snapshot-taken-at-startup.md)）。**再発しないことを直った証拠にしないこと** — 4 回とも実作業の中でしか出ていない
- **カーソルが壊れたら、ログの `カーソル:` の行を見る。** この版から「元が空だったので着色しなかった」「作った着色が空だったので捨てた」「複製を採れなかった」を**名指しで言う**ようになった。v0.8 では何も残らなかった
- **egui が 2 版上がった。** [ADR 0074](decisions/0074-egui-0-36-and-the-upstream-pr.md)。**H-3（見た目）を「前回から変更なし」で省略できない版である** — 見た目の土台そのものが入れ替わっている
- **設定ウィンドウに行が 2 つ増えた**（`change_cursor_color` と `cursor_color`）。**利用者から見える変化はこれだけ**である。**M-7** で見る
- **MSIX のアイコンは面ごとに描き方が違う。** スタートアップ一覧は**プレート（下地）付き**、タスクバーは**下地なし**。片方だけ直っている状態がありうるので、**P-10 は 3 面とも見る**
- **リリースビルドで通す。** `build.ps1` は既定で `-Configuration release`
- **自動テストはパッケージ版を見ていない。** 動かすのは素の `winremap.exe` である。これが P-1〜P-10 の存在理由

### ⚠ P-9 の前提が、いま欠けている

**2026-08-11 に、v0.8 の P-5（アンインストール）で Store 版 0.8.0 を消した。** そのままだと **v0.9 の P-9（0.8.0 → 0.9.0 の更新）は測れない** — 更新元が機械に無い。

1. **0.9.0 を提出する前に、Store から 0.8.0 を入れ直しておくこと**
2. **入れ直したら、それ以降 `packaging\msix\build.ps1 -Register` を通さないこと。** 同じ Identity を登録するので、入れ直した 0.8.0 が置き換わって振り出しに戻る（v0.9 Phase C の確認は 2026-08-11 に済んでいるので、もう通す用事は無いはずである）

**この形の取りこぼしは v0.7 で 1 度実際に起きている**（自動更新で 0.7.0 の P-9 が永久に測れなくなった）。[§6.2](#62-store-側p-8p-9) を先に読むこと。

---

## 1. 毎リリースの手順

```powershell
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo test                              # 142 件（v0.8 から 5 件増えた）
.\site-src\build.ps1 -Check             # site/ が site-src/ から作った通りか（ADR 0066）
cd tests\ui
.\run-vm-ui-test.ps1                    # ゲスト VM での UI 自動テスト一式（全 10 件・121 チェック）
cd ..\..
cargo build --release --features test-inject
.\tests\acceptance\probe-ime-cursor.ps1 # カーソルの画素を直接読む検査（10 項目、ADR 0067・0073）
cargo build --release                   # 受け入れは素の配布ビルドで行う
```

**自動側は Claude Code が回す。所要時間と、人の作業を邪魔するかは [v0.8 §1](../v0.8/03_acceptance-checklist.md#1-毎リリースの手順) と同じ**（全部で 30 分弱、うち 26 分が VM）。**`probe-ime-cursor.ps1` の間は機械に触らないこと。**

### この版で自動側に増えたもの

| 検査 | 増えた項目 | 何を見ているか |
|---|---|---|
| `probe-ime-cursor.ps1` | **`repeated-toggles-never-empty-the-tint`**（8 → 10 項目。もう 1 件は v0.8 で増えた画面上の確認） | **M-2 は初回の遷移では出ない。** しばらく使ったあとに出て、一度出ると空が空を作って戻らなくなる。IME の on/off を 3 往復し、**毎回**「登録されたカーソル」と「画面に描かれているカーソル」の両方を見る |
| `cargo test` | `cursor::tests` 2 件、`config::draft` 3 件 | 空の着色を捨てる判定が**届く経路であること**（縁だけのカーソルは、空でない元から空を作れる）と、増えた 2 設定が下書きを往復すること |

> **VM の UI テストの `element-count` は、増減を捕まえられない。** しきい値が `>= 40` で、実測は **54**（この版で設定ウィンドウに 2 行増えても余裕で通った）。40 を割るのは要素が 14 個消えたときで、それは「壊れた」より「開いていない」に近い。**行が 1 つ静かに消えても緑のままである。** この版では直していない — 直すなら基準値との差で見る形になる。[§7](#7-記録欄散文) に判断を残すこと。

1. 上が緑であること。**落ちたら手動へ進まない**
2. 手動の項目は **Claude Code との対話で進める**。Claude Code で:

```
/acceptance docs\v0.9\03_acceptance-checklist.md
```

   **司会は Claude Code が務める。** 環境の準備（設定の退避と配置、WinRemap の起動）を行い、項目を 1 件ずつ読み上げる。**人がすることは、実キーボードで打って、見えたことを自分の言葉で言うこと**だけ。4 値への分類と記録の文は Claude Code が案を出し、**確認を取ってから** [§8](#8-対話ハーネスの記録) の記録欄へ追記する
3. **キーは実キーボードで打つこと。** ハーネスは 1 つもキーを注入しない
4. **オーナーが「消えていると思います」と言ったら、機械で確かめてから書くこと。** 額面どおり記録に書いて外したことが v0.8 で 4 回ある。`Test-Path` や `Get-AppxPackage` で答えが出るものは、答えを出してから書く
5. §7 に、その回の全体的な判断（省略した項目とその理由など）を散文で書く
6. **Store 側（P-8・P-9）は認定通過後**に `-Resume` でもう一度回す。**順序は P-9 → P-8**（[§6.2](#62-store-側p-8p-9)）

---

## 2. カーソルまわり（M-1〜M-7）

### 2.1 事前準備

**`-Prepare M`。** 設定は [`tests/acceptance/ime-cursor.toml`](../../tests/acceptance/ime-cursor.toml)。退避・配置・起動・後片付けはハーネスがやる。**メモ帳と、暗い背景のアプリ（Zed など）を開いておくこと** — M-2 は背景の明るさで答えが変わる。

### 2.2 項目

**M-1〜M-6 は [v0.8 §2.3](../v0.8/03_acceptance-checklist.md#23-ime-をマウスカーソルの色で示すadr-0067) と同一。M-7 はこの版で新設。**

| # | 何を見るか | 手順 | 通過条件 |
|---|---|---|---|
| **M-1** | **IME のオン・オフで色が変わる** | メモ帳で IME をオンにし、マウスを動かす。オフに戻す | ①オンで**矢印に色が付く** ②オフで**素に戻る** ③切り替えてから色が変わるまでが**待たされる感じでない** |
| **M-2** | **入力欄の上でも変わる**（I ビーム）— **この版の主題** | 同じ状態で、テキストの上へマウスを乗せる。**そのあと、しばらく普通に使う** | ①I ビームにも色が付く ②**黒い四角にならない**（形が残っている） ③**暗い背景のアプリ**（Zed など）でも**見失わない**（白い縁が効いている） ④**何も描かれないカーソルにならない**（v0.8 で 4 回落ちた症状） |
| **M-3** | **管理者権限のウィンドウの上では変わらない** | 管理者権限のターミナルを前面にして IME をオン | **色は変わらず、パネルも出ない。** リマップが効かないのと同じ理由（UIPI）である |
| **M-4** | **強制終了すると色が残り、次の起動で戻る** | IME をオンにして色が付いた状態で `Stop-Process -Name winremap -Force`。そのあと WinRemap を起動し直す | ①**色が残る** ②トレイに居ないことと合わせて「異常終了した」と読める ③**起動し直すと戻る**（何も操作せずに） |
| **M-5** | **独自のカーソルテーマが保たれる** | カーソルのテーマやサイズを変えている場合、色を付けてから戻す | ①色付きでも**自分のテーマの形**である ②戻したあと**元のテーマに戻る**。**テーマを変えていなければ「未実施」** |
| **M-6** | **パネルを出さずにカーソルだけ使える** | 設定を `enabled = false` ＋ `change_cursor_color = true` にして再読み込み | ①「あ」パネルは出ない ②カーソルの色は変わる（2 つが独立していること） |
| **M-7** | **設定ウィンドウでカーソルの色を編集できる**（**この版で新設**） | トレイ →「アプリの設定」→ `[ime_indicator]`。表示を見てから「編集」に入り、**`enabled` のチェックを外す** | ①表示・編集の**両方**に「マウスカーソルに色を付ける」と「カーソルの色」がある ②**`enabled` を外しても 2 行が消えない**（カーソルの色は `enabled` と独立 — [ADR 0067](../v0.8/decisions/0067-ime-cursor-color.md)） ③色の欄が `#rrggbb` の形で、**その色の見本**が隣に出る ④保存せずに閉じても、開き直すと元の値のまま |

> **M-2 の④は、その場では測れない。** v0.8 の 4 回はいずれも**実作業の中**で出ており、狙って再現できたことは一度もない。この項目で見るのは①〜③で、④は**数日使ったうえでの報告**として [§7](#7-記録欄散文) に書く。**「出なかった」を通過の根拠にしないこと** — 自動側の `repeated-toggles-never-empty-the-tint` のほうが証拠である。
>
> **もし出たら、その場でログを見ること。** ログウィンドウ（または `--debug`）に `カーソル:` で始まる行がある。この版からは「**元が空だったので着色しなかった**」「**作った着色が空だったので捨てた**」「**複製を採れなかった**」を名指しで言う。**壊れたカーソルのホットスポットが `(9,10)`、正常なものが `(8,9)` だった**という手掛かりも残っている（[調査ノート](../v0.8/notes/20260809_m2-empty-ibeam.md)）。

---

## 3. `--debug` の専用コンソール（C-1〜C-5、v0.8 から継承）

**5 件とも [v0.8 §2.2](../v0.8/03_acceptance-checklist.md#22---debug-の専用コンソールadr-0068) と同一で、この版では手が入っていない。** 設定を問わないので準備も要らない。

| # | 何を見るか | 手順 | 通過条件 |
|---|---|---|---|
| **C-1** | **起動時のログが 1 行目から読める** | ターミナルで `.\winremap.exe --debug` | ①**別のコンソールウィンドウ**が開く（タイトル `WinRemap --debug`） ②`WinRemap v0.9.0 を起動しました` から**すべて**出ている ③起動元のターミナルのプロンプトと**重ならない** |
| **C-2** | **終了時のログが読める** | C-1 の状態でトレイ →「終了」 | ①`[操作] 終了` とセッション終了行が出る ②**ウィンドウが消えない**（待機している） ③Enter か閉じるボタンで終わる |
| **C-3** | **窓を閉じると WinRemap も終わる** | C-1 の状態で、動作中に `--debug` の窓を閉じる | ①WinRemap が終了する（トレイから消える） ②**カーソルの色を使っていた場合、色も戻る** |
| **C-4** | **リダイレクトすると従来どおり** | `Start-Process .\winremap.exe -ArgumentList '--debug' -RedirectStandardOutput log.txt`（**シェルの `>` では確かめられない** — [v0.8 §2.2 の表](../v0.8/03_acceptance-checklist.md#22---debug-の専用コンソールadr-0068)） | ①**窓は開かない** ②`log.txt` に同じ内容が入る ③**待たない**（プロンプトが戻る）④**常駐が続く** |
| **C-5** | **`--help` と `--version` は起動元へ** | `.\winremap.exe --help` と `--version` | 起動元のターミナルに出る（専用の窓を開かない） |

> **C-1 の②で、この版から 1 行増えている。** `カーソル: 復元用に素の複製を 2 個持ちました`。**これは失敗ではなく成功の記録である**（[ADR 0073](decisions/0073-restore-from-a-snapshot-taken-at-startup.md) 決定 5 の趣旨 — 安全網が張れているかを見に行けること自体が直しの一部）。**`2 個中 0 個` や `1 個` と出ていたら、そこを記録に書くこと。**

---

## 4. 手動最小集合（H-1〜H-10）

**`-Prepare H`。** 測る設定は [`tests/acceptance/manual-minimum.toml`](../../tests/acceptance/manual-minimum.toml)。

> **`C-h` が 1 文字消えることは、それだけでは判定にならない。** シェルに Emacs キーバインドを当てていると WinRemap 抜きでも同じことが起きる。**ログの `[判定] C-h … → Back … に置換` の行**と、**`C-a` で行頭へ動くこと**（素の Windows なら全選択）の 2 つで見ること。
>
> **H-6 だけはこの準備の対象外である。** 測るのは**インストール済みの** `winremap.exe` であって、`-Prepare H` が起動する `target\release` のものではない。

| # | 何を見るか | 手順の要点 | v0.9 での見どころ |
|---|---|---|---|
| **H-1** | **発端の問題**が配布ビルドで直っていること | PHPStorm の内蔵ターミナルで `C-h`。1 文字削除になる。**リリース用のバイナリ**と実キーボードで行う | 変更なし。リマップ本体にはこの版で手が入っていない |
| **H-2** | **止まらない・遅れない** | 設定・ログウィンドウを開いたまま、普通の速さで数分間文章を打つ。取りこぼし・二重入力・遅延・スクロールの遅れが無いこと | **カーソルの色を有効にしたまま**行う。**この版でカーソルの復元経路が作り直された** — IME を何度も切り替えて、打鍵が引っかからないかを見る |
| **H-3** | **見た目**（1 回で全部見る） | 設定・ログウィンドウとトレイメニューを、**ライトとダークの両方**で開く。罫線・余白・アイコン・注記の印が崩れず判読でき、豆腐（□）が無いこと。日本語表示で未翻訳が無いこと | **省略してはならない版である。** egui が 0.35.0 → 0.36.1 に上がっており、**見た目の土台そのものが入れ替わっている**（[ADR 0074](decisions/0074-egui-0-36-and-the-upstream-pr.md)）。設定ウィンドウには行も 2 つ増えている |
| **H-4** | **放置時の CPU** | 両ウィンドウを閉じて数分放置し、WinRemap の CPU がほぼ 0 であること | egui の載せ替えで再描画の条件が変わっていないかを、この版だけ意識して見る |
| **H-5** | **IME**（1 回で全部見る） | インジケーターを有効にして、メモ帳・ブラウザ・設定アプリ（UWP）で IME をオン。表示位置・フォーカスを奪わないこと・シェル面で出ないこと。設定ウィンドウの入力欄で日本語変換ができること | パネルとカーソルは同じ観測から出ている。**カーソル側だけ作り直した**ので、**片方だけ正しく片方が遅れる**ことがないかを見る |
| **H-6** | **インストーラー経路** | インストールし、スタートメニューから起動、自動起動を有効にして再サインイン。黒い窓が一瞬も光らないこと | **v0.8 では 2 度「未実施」で送った項目である**（リリース工程で回す判断）。**この版では実施すること。** P-4 とは**別のサインイン**で行う — 同じサインインで両チャネルを自動起動させると、単一インスタンスのダイアログが出て交絡する |
| **H-7** | **マクロ記憶**（1 回で全部見る） | 記憶 → 数文字 → 記憶終了 → 再生。再生しながら別のキーを打ってもリマップが止まらないこと。無効化・リロードで記憶が中断されること。終了後に記憶が消えていること | 変更なし |
| **H-8** | **バナー** | 記憶中のバナーが前面アプリのモニター下部中央に出て、クリックが透過し、Alt+Tab に出ないこと。**マルチモニターと管理者権限ウィンドウ**もここで見る | 変更なし。ただしバナーも egui の上に乗っている（H-3 と同じ理由） |
| **H-9** | **文書** | README（en/ja）とヘルプサイトが、その版の実挙動と食い違っていないこと | **[§5](#5-h-9文書で見る観点) に、この版の観点をまとめてある。** M-2 の「既知の問題」を消すかどうかがこの版の焦点 |
| **H-10** | **ターミナルから起動しても常駐が続くこと** | ターミナルで `winremap.exe` → メモ帳で `C-h` → **そのターミナルを閉じる** → 再び `C-h`。次に `--debug` 付きで同じことを行い、**`--debug` が開いた窓のほう**を閉じる | ①**無印は、起動元のターミナルを閉じても効き続ける** ②**`--debug` も効き続ける** ③**`--debug` の窓を閉じると終了する** |

> H-1・H-2・H-7 は**リマップ本体**の確認で、落ちたらリリースしない。H-3〜H-6・H-8〜H-10 は版によって影響を受けない場合があるが、**判断を毎回書き残す**こと（「前回から変更なしのため省略」も記録に残せば可 — ハーネスでは「未実施」＋理由）。**ただしこの版の H-3 と H-6 は上記のとおり省略しない。**

---

## 5. MSIX 固有項目（P-1〜P-10）

### 5.1 事前準備

**[v0.6 §3.1](../v0.6/03_acceptance-checklist.md) の手順がそのまま通用する。** 退避 → パッケージとデータの削除 → `build.ps1 -Register` の順で、`%APPDATA%\winremap` の有無によって通る経路が変わる（[ADR 0061](../v0.6/decisions/0061-packaged-config-path.md)）。

> **退避したものは必ず戻す。ハーネスはこの節を自動化しない。**
>
> **スタートメニューに `WinRemap` が 2 つ並ぶ状態になったら、見るものを取り違える。** インストーラー版と MSIX 版が両方入っているときに起きる。2026-08-10 に P-1 をこれで測り間違えた。**測る前に、動いているプロセスのパスを確かめること** — MSIX 版だけを確実に起動するなら次を使う。
>
> ```powershell
> explorer.exe shell:AppsFolder\SUGANUMADaiki.WinRemap_pktmgf1zdhxe0!WinRemap
> ```

### 5.2 項目

| # | 何を見るか | 手順の要点 | 通過条件 |
|---|---|---|---|
| **P-1** | **Store 版の初回起動** | 設定が無い状態でスタートメニューから起動。ログを開き、メモ帳で `C-h` | ①常駐する ②ログに**既定設定を作った行** ③`C-h` が 1 文字削除 |
| **P-2** | **設定パス（新規導入）** | トレイ → 設定。アドレスバーを読み、「フォルダーを開く」 | ①`…\Packages\SUGANUMADaiki.WinRemap_pktmgf1zdhxe0\LocalCache\Roaming\winremap` を指す ②Explorer が実際にそこを開く |
| **P-3** | **設定パス（インストーラー版からの移行）** | 設定を戻してから起動し直し、設定ウィンドウを開く | ①`%APPDATA%\winremap` を指す ②**自分のキーマップが出ている** ③「フォルダーを開く」でそこが開く |
| **P-4** | **スタートアップ** | 設定 → アプリ → スタートアップで WinRemap をオン → サインアウト／サインイン | ①一覧に出る ②既定は**オフ** ③オンにすると常駐し、**黒い窓が光らない** |
| **P-5** | **アンインストール** | `Get-AppxPackage -Name SUGANUMADaiki.WinRemap \| Remove-AppxPackage` | ①スタートメニューから消える ②`%LOCALAPPDATA%\Packages\…` が消える ③**`%APPDATA%\winremap` の設定は残る** |
| **P-6** | **単一インスタンス** | パッケージ版と GitHub 版を両方起動（両順序） | 2 つ目が**自力で終了**し、トレイアイコンが 1 つのまま。**コンソールから起動した 2 つ目は文言を出して終わり、シェルから起動した 2 つ目はダイアログを出す** — どちらも正常である |
| **P-7** | **管理者権限ウィンドウ** | 管理者ターミナルにフォーカスして `C-h` | リマップが**効かない**（UIPI）。GitHub 版と同じ結果。**カーソルの色も変わらず、パネルも出ない** |
| **P-8** | **Store 経由インストール**（**認定通過後**） | ストアページから実際にインストールする | ①**SmartScreen の警告が出ない** ②発行元が **SUGANUMA Daiki** ③P-1 と同じ初回起動 |
| **P-9** | **更新（0.8.0 → 0.9.0）** | **Store 版**: 0.8.0 が入った状態で、認定通過後に Microsoft Store の「ライブラリ → 更新」から更新する。**GitHub 版**: 0.8.0 のインストーラーが入った状態で 0.9.0 のインストーラーを実行する | ①**設定ファイルがそのまま残る**（キーマップが消えていない） ②ログのセッション行と設定ウィンドウが **0.9.0** と出る ③**旧版が残らない**（一覧に 1 つ、常駐も 1 つ） ④**自動起動の設定が保たれる** |
| **P-10** | **アイコンが正しく出る**（**この版で新設**） | パッケージ版を入れた状態で 3 か所を見る。①**設定 → アプリ → スタートアップ** ②**タスクバー**（起動して設定ウィンドウを開く） ③**スタートメニュー**（「WinRemap」で検索） | **3 か所とも WinRemap のロゴが読めること。** 塗りつぶされた四角や、下地と同色で潰れた状態でないこと |

> **P-10 は 3 か所とも見る必要がある。面ごとに描き方が違うからである**（2026-08-11 に、3 回入れ直して分かった）。
>
> | 面 | 使うもの | 効くもの |
> |---|---|---|
> | **タスクバー** | `_altform-unplated` のアセット | **`resources.pri`**（これが無いと、置いてあっても引かれない） |
> | **スタートアップ一覧** | **プレート（下地）の上にロゴ** | **`BackgroundColor`**。`transparent` は「透明」ではなく「**アクセントカラーを使う**」という意味で、青いロゴが青い下地に乗ると塗りつぶしに見える |
>
> **片方だけ直っている状態が実際にあった。** `resources.pri` を足した段階で**タスクバーだけ直り、スタートアップ一覧は青いまま**だった。**1 か所見て通過にしないこと。**

---

## 6. 持ち越しと「測れない」項目

### 6.1 S-6（配列の切り替え）— v0.7 から持ち越し

別配列のキーボード（JP 106 ↔ US 101）が手元に無く、v0.7・v0.8 とも実施できなかった。**v0.9 の作業ではない。** ハーネスでは「測れない」として残す。

| # | 何を見るか | 手順 | 通過条件 |
|---|---|---|---|
| **S-6** | **配列の切り替えを再読み込みで拾う**（**キーボードがある場合のみ**） | 別配列のキーボードに差し替え、トレイ → **再読み込み**。設定ウィンドウの「使えるキー名」と `"C-;"` を見る | ①刻印の一覧が**新しいキーボードのものに変わる** ②`"C-;"` が**新しい配列の `;` のキー**に解決する ③差し替えただけで再読み込みしなければ**変わらない**（仕様どおり） |

### 6.2 Store 側（P-8・P-9）

0.9.0 の認定通過後にしか測れない。**認定を待つ間にリリースを止めない** — 通過後に `-Resume` で埋める。

**通過後は P-9 → P-8 の順で行う。** P-8 は**アンインストールして新規インストールする**項目なので、先に通すと **P-9 が要求する「0.8.0 が入っている状態」が消える**。Store は旧バージョンを配らないので、**更新経路は二度と測れなくなる**。

**その日の最初の作業として P-9 を回すこと。** Microsoft Store の「アプリの更新」を切れれば確実だが、**この設定には管理者権限が要る**（2026-08-10 に判明。オーナーの権限では切れなかった）。**切れない機械では、通過の連絡を見たら他の何よりも先に、ライブラリから手で更新するしかない。** 0.8.0 はそれで測れたが、**守ったのは手当てではなく速さである**。

**更新前に自動起動をオンにしておくこと。** 通過条件④は「自動起動の設定が保たれる」だが、**既定のオフのままでは「オフがオフのまま」で何も測れない**。

> **そして [§0](#-p-9-の前提がいま欠けている) のとおり、この版はそもそも更新元が機械に無い。** 0.9.0 を提出する前に **Store から 0.8.0 を入れ直す**こと。

---

## 7. H-9（文書）で見る観点

**「嘘になっていないか」を見る**のであって、文章の巧拙を見るのではない。

**実挙動が変わったとき、直す場所は README とヘルプサイト（`site-src/`）の 2 つある。** v0.8 では**同じ落ち方を 3 回**した。3 回とも README は直っていて、`site-src/` への反映だけが漏れていた。**README が直っているからサイトも直っている、と読まない。**

| 見るもの | 確認 |
|---|---|
| **M-2 の「既知の問題」**（README 日英・`ime.html` 日英） | **この版の焦点。** v0.8 で「着色した I ビームが描かれなくなることがある」と書いた。**直っていれば消す。直っていなければ残す**（[開発計画 §6](01_development-plan.md)）。**M-2 の結果が出るまで、この行は判断できない** |
| ヘルプサイト `config.html`（日英） | **この版で増えた設定が載っている**（下記のコマンドで数え上げる）。README が「Every option」として指すページであり、**ここに無い設定は利用者から見れば存在しない** |
| `README.md` / `README.ja.md` | IME インジケーターの節に **`change_cursor_color` / `cursor_color`** がある。**「色が残り、トレイに居ない ＝ 異常終了」**の読み方がある |
| ヘルプサイト `ime.html`（日英） | 同じ 3 点（設定・`enabled` と独立・異常終了の読み方）がある |
| ヘルプサイト全体 | **`site/` を直接編集していない**こと（`site-src\build.ps1 -Check` が緑）。言語切り替えリンクが**同じページの対訳**を指す |
| [02_config-spec.md](../v0.1/02_config-spec.md) §6 | 増えた設定の行があり、**既定値が実装と一致**している |
| リンク切れ | 下記の**リンク切れの検査**が何も出力しないこと |

**この版で増えた設定を数え上げる。** 設定名を受け付けているのは `src/config/raw.rs` の構造体であり（`deny_unknown_fields` が付いているので、ここに無い名前は読み込みで弾かれる）、**前の版のタグとの差分がそのまま「増えた・改名した設定」の一覧**になる。

```powershell
git diff v0.8.0..HEAD -- src\config\raw.rs
```

> **v0.9 はこの差分が空になる見込みである**（利用者から見える新機能を入れない版なので）。**空であることを確かめるのも、この項目の仕事である** — 空でなければ、入れないと決めたものが入っている。

**リンク切れの検査:**

```powershell
Get-ChildItem docs -Recurse -Filter *.md | ForEach-Object {
  $dir = $_.DirectoryName
  foreach ($m in [regex]::Matches((Get-Content $_.FullName -Raw), '\]\(([^)\s]+)\)')) {
    $u = $m.Groups[1].Value
    if ($u -match '^(http|mailto:|#)') { continue }
    if (-not (Test-Path (Join-Path $dir ($u -split '#')[0]))) { "$($_.Name): $u" }
  }
}
```

---

## 8. 記録欄（散文）

| 日付 | 範囲 | 結果 |
|---|---|---|
| （未実施） | | |

---

## 9. 対話ハーネスの記録

[`tests/acceptance/run-acceptance.ps1`](../../tests/acceptance/run-acceptance.ps1) が下のマーカーの間に追記する（[ADR 0069](../v0.8/decisions/0069-interactive-acceptance-harness.md)・[ADR 0070](../v0.8/decisions/0070-agent-led-acceptance.md)）。見出しには**その回の環境**（キーボード配列・版・OS）と、**誰が答えたか**が刻まれる。**手で書き換えないこと** — 追記のみで、同じ項目を後日やり直せば新しい行が増える。「測れない → 認定通過後に通過」という経緯が読めることが、この欄の値打ちである。

<!-- harness:begin -->
<!-- harness:end -->
