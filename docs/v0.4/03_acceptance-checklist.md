# v0.4 受け入れチェックリスト

> v0.1〜v0.3 の[受け入れチェックリスト](../v0.3/03_acceptance-checklist.md)に対する差分。v0.4 Phase A（設定 GUI 編集・エクスプローラー風画面）で追加・変更した挙動のみを扱う。
> v0.1〜v0.3 の全項目もリリース前に再実施すること（リマップ本体の回帰確認）。
> 設計の正: [02_config-editing-design.md](02_config-editing-design.md)（編集・保存）、[04_config-editing-screens.md](04_config-editing-screens.md)（画面）、ADR [0049](decisions/0049-config-edit-draft-model.md)〜[0051](decisions/0051-notify-file-watch.md)。

- 作成日: 2026-07-22
- 作成: Claude Code（AI モデル: claude-opus-4-8）／実施・記録: オーナー

## 実施上の前提（重要）

- **WinRemap を 1 つだけ動かした状態で行う**（インストール版が常駐していれば先に終了。二重フックは挙動不定）
- 本チェックは**コメント・空行・並び順のある設定ファイル**で行う。最低限、次の形を含めること:

```toml
# ファイル先頭のコメント

[macro]
delay_ms = 8  # ペースの同行コメント

[ime_indicator]
enabled = true

[[keymap]]
name = "emacs"  # 名前の同行コメント
application = [
  "notepad.exe",  # アプリの同行コメント
]

[keymap.remap]
"C-h" = "Back"      # 規則の同行コメント
# 独立コメント（前後を空行で挟む）

"S-C-h" = "Delete"
"C-t" = ["C-Right", "C-Left"]  # マクロ

[[keymap]]
application = ["*"]
exclude = ["game.exe"]

[keymap.remap]
CapsLock = "LCtrl"
```

- 同じフォルダーに 2 つ目の toml（例 `backup.toml`、中身は有効な設定）を置いておく（切替の確認用）

## A — 画面骨格（エクスプローラー風・オーナー指示 2026-07-22）

| # | 手順 | 期待する結果 | 結果 |
|---|---|---|---|
| A-1 | トレイ →「設定」でウィンドウを開く | ヘッダー＝アドレスバー（📁フォルダーパス › ファイル名プルダウン ↻）、右端に［✎ 編集］。**全要素が上下中央揃え** | |
| A-2 | ヘッダーを見る | バージョン表示は**ヘッダーに無い**。最下部のステータスバーに `WinRemap v0.4.0 │ 起動: <日時> │ 読み込み完了しました` | |
| A-3 | 左ナビゲーションを見る | ⚙全体設定 / ⌨キーマップ見出し / 各キーマップ（1 段インデント・アイコン付き）。開閉マークは無い | |
| A-4 | キーマップの**テキストの上**をクリック | 1 クリックで選択され、行全体が灰色に塗られる（ホバー時は薄い灰色が行全体に出る） | |
| A-5 | 選択を切り替える | 中央ペイン先頭のパンくずが `設定 > 全体設定` / `設定 > キーマップ > emacs` と変わる。パンくず下に区切り線は**無い** | |
| A-6 | キーマップ詳細を見る | 大見出しの重複表示は無く、`名前 name = emacs` の行とその下に同行コメントが出る | |
| A-7 | プルダウンを開く | フォルダー内の toml が並び、使用中に ✓。最下段に余白付きで「テキストエディタで開く」「フォルダーを開く」 | |
| A-8 | 「フォルダーを開く」を押す | エクスプローラーで設定フォルダーが開く | |

## B — ファイル切替・変更ありマーク（ADR 0050 / 0051）

| # | 手順 | 期待する結果 | 結果 |
|---|---|---|---|
| B-1 | プルダウンで `backup.toml` を選ぶ | アドレスバーの表示が切り替わり、リロードが走る（ステータスバーに「読み込み完了しました」、キーマップ一覧が backup の内容に）。**リマップ挙動も backup に変わる** | |
| B-2 | 切替後、トレイ「設定を再読み込み」を押す | backup.toml が再読込される（トレイも新パスを見ている＝ADR 0050） | |
| B-3 | 使用中ファイルを外部エディタで保存する（窓は開いたまま） | アドレスバーのファイル名に **●** がほぼ即時に付く（notify。数秒待っても付かない場合はポーリングのフォールバックで 2 秒以内） | |
| B-4 | ↻ を押す | リロードされ ● が消える | |
| B-5 | 中身が壊れた toml に切り替える | ライブ表は据え置き（リマップ継続）、エラー通知が出る。現行のリロード失敗と同じ扱い | |
| B-6 | `cargo run -- --config .\examples\suganuma.toml` のように**相対パス**で起動 | 起動ログに絶対パスが出て、アドレスバーに親フォルダーとその中の toml 一覧が表示される | |

## C — 編集と保存（B2 / ADR 0049 / ADR 0036）

| # | 手順 | 期待する結果 | 結果 |
|---|---|---|---|
| C-1 | ［✎ 編集］を押す | ヘッダー帯が編集色になり、右端が同じ位置で［💾 保存］［↶ 元に戻す］に変わる。プルダウンは無効化（ツールチップに理由）、↻ は消える。パンくず右端に「● 編集中 —」 | |
| C-2 | 規則の出力を 1 箇所だけ変えて保存 | 閲覧モードへ戻り即反映（リマップ挙動が変わる）。ファイルを開くと**変えた 1 箇所以外は 1 文字も変わっていない**（コメント・空行・並び順・`"S-C-h"` の綴りが残る） | |
| C-3 | 規則を追加して保存 | `[keymap.remap]` の**末尾**に追記される | |
| C-4 | 「独立コメント」の直後の規則（`S-C-h`）を ✕ で削除して保存 | 規則と**密着コメントだけ**が消え、空行で離れた「独立コメント」は残る | |
| C-5 | 左ナビの ↑↓ でキーマップを並べ替えて保存 | `[[keymap]]` テーブルが**コメント・remap ごと丸ごと**移動する。`[macro]` 等は動かない | |
| C-6 | ＋でキーマップを追加（名前・アプリ・規則を入力）して保存 | ファイル末尾に新しい `[[keymap]]` セクションが書かれる | |
| C-7 | −でキーマップを削除して保存 | そのセクションが remap ごと消える。他は不変 | |
| C-8 | 全体設定で delay スライダー・IME 設定を変えて保存 | 該当キーだけ書き換わり、同行コメントが残る | |
| C-9 | v0.1 綴り `macro_delay_ms = 4` のファイルで delay を変えて保存 | **同じ綴りのまま**値だけ変わる（`[macro]` へ移動しない） | |
| C-10 | 何か変更 → ［↶ 元に戻す］ | 破棄されて閲覧へ。ファイル不変 | |
| C-11 | 未保存の変更がある状態で ✕（閉じる） | 閉じずにフッターで「保存せずに閉じますか?」。［閉じる］で破棄して非表示、［キャンセル］で編集継続 | |
| C-12 | 編集中にトレイ「設定を再読み込み」 | 編集内容（ドラフト）は**消えない**（ADR 0049 決定 3） | |

## D — 検証と入力補助（B3）

| # | 手順 | 期待する結果 | 結果 |
|---|---|---|---|
| D-1 | 規則の入力欄に `C-h` と打つ | 直下に薄字で「ⓘ Ctrl + h」 | |
| D-2 | `A-x h` と打つ | 「ⓘ Alt + x に続けて h（2 ストローク）」 | |
| D-3 | 出力欄に `C-Right, C-Left` と打つ | 「ⓘ Ctrl + Right → Ctrl + Left（マクロ）」 | |
| D-4 | 入力欄に `Bak` と打つ | 枠が警告色になり「⚠ 未知のキー名 "Bak"。"Back" のことですか?」。**入力は続けられる** | |
| D-5 | 不正な記法のまま［💾 保存］ | 保存されず、フッターに「⚠ n 件の問題があります: <行番号つきの先頭 1 件>」。2 件以上なら［次へ ▸］で巡回 | |
| D-6 | 規則表の下の `?` を押す | 使えるキー名の一覧（修飾/文字/ファンクション/特殊）がポップアップし、外をクリックで閉じる | |
| D-7 | 編集中の規則表を見る | 元ファイルの同行コメントが読み取り専用のコメント列に出る | |
| D-8 | 記憶キー（`record_start` 等）・`trigger_keys` の欄 | 同じ ⓘ/⚠ の逐次検証が効く（trigger_keys はカンマ区切りで複数可） | |

## E — 前面アプリから取得・外部変更（B4）

| # | 手順 | 期待する結果 | 結果 |
|---|---|---|---|
| E-1 | 「今の前面アプリから取得」を押し、3 秒以内にメモ帳へ切り替える | ボタンが「3… 2… 1… 対象アプリを前面にしてください」と数え、`notepad.exe` が対象アプリの末尾に追加される | |
| E-2 | 既に載っている exe で同じ操作（大文字小文字違いも） | 重複追加されない | |
| E-3 | `application = ["*"]` のキーマップを開く | 取得ボタン自体が**出ない** | |
| E-4 | 編集中に外部エディタで同じファイルを書き換え → ［💾 保存］ | 黙って上書きせず、フッターに「設定ファイルが WinRemap の外で変更されています。」＋［上書き保存］［読み直す（編集を破棄）］［キャンセル］。各ボタンが文言どおり動く | |
| E-5 | 読み取り専用にしたファイルで保存 | フッターに「保存できませんでした: <理由>」。編集内容は失われない | |

## F — 不変条件の回帰（ブリーフ §7）

| # | 手順 | 期待する結果 | 結果 |
|---|---|---|---|
| F-1 | 編集モードのまま、別アプリで連続タイプ | リマップが**停止・遅延しない**（不変条件 1・2） | |
| F-2 | 保存の瞬間を挟んで連続タイプ | 取りこぼし・二重入力が無い（atomic swap） | |
| F-3 | 編集して**保存しない**間、ファイルの mtime を監視 | ［保存］まで**ファイルは一切書き変わらない**（tmp ファイルも保存時のみ） | |
| F-4 | 名前・コメント欄ではなく**規則の入力欄**に IME で日本語を打つ | クラッシュせず ⚠ 表示になるだけ。IME 変換中の下線表示が出る（ADR 0034） | |
| F-5 | `--lang en` で起動 | 新しい UI（アドレスバー・編集・フッター・ステータスバー）が全て英語で出る | |
| F-6 | ログウィンドウを開いたまま一連の操作 | ［操作］行（編集/保存/切替/取得）が記録される。**キー入力の内容がディスクに書かれることは無い**（不変条件 6） | |

## 記録欄

- 実施日: ____ / 実施者: ____ / 版: `feature/config-edit` @ ____
- 全項目 OK なら Phase A 完了。NG はフェーズに戻して修正後、該当項目のみ再実施
